# 👋 시작하기 (START HERE)

이 파일은 **이 프로젝트를 처음 여는 날, 또는 한참 만에 돌아온 날** 읽는 가이드입니다.
이미 진행 중이라면 `/recap` 슬래시 커맨드를 쓰는 게 더 빨라요.

---

## 이 프로젝트는 뭔가요

**2D 사이드스크롤 MMORPG**를 만듭니다. Unity 클라이언트 + .NET 권위 서버.
목표는 두 가지:
1. 실제로 돌아가는 게임을 만드는 것
2. **백엔드/네트워킹 실력을 실전으로 키우는 것** (이게 더 중요)

따라서 코드 빨리 쏟아내기보다, **왜 이렇게 만드는지 이해하면서** 한 단계씩 갑니다.

---

## Harness 구성 요소 한눈에

```
project-root/
├── CLAUDE.md            ← 헌법. 절대 원칙 + 사용자 컨텍스트.
├── docs/                ← 프로젝트의 뇌
│   ├── PRD.md            "무엇을" 만들지
│   ├── ARCHITECTURE.md   "어떻게" 만들지
│   └── ADR.md            "왜" 이렇게 결정했는지
├── phases/              ← 작업 쪼개기
│   └── M1-foundation/    마일스톤별 Phase 파일들
├── .claude/
│   ├── agents/           서브에이전트 6개 (도메인별 전문가)
│   ├── commands/         슬래시 커맨드 (학습 + 작업)
│   └── hooks/            자동 가드레일
└── client/, server/, shared/, tools/
```

각 디렉토리(`client/`, `server/`, `shared/`)에도 별도 `CLAUDE.md`가 있어서
해당 영역에서 일할 때 자동 적용됩니다.

---

## 첫날 추천 코스

### Step 0: 환경 준비 (15~30분)

- [ ] .NET 10 SDK 설치 (`dotnet --version`으로 확인)
- [ ] Unity 6.4 LTS 설치 (Unity Hub 통해)
- [ ] PostgreSQL 또는 Docker Desktop 설치
- [ ] Git 초기화 (`git init`)
- [ ] VSCode 또는 Rider 같은 IDE

### Step 1: 헌법 + 문서 한 번 읽기 (30분)

순서대로 통독:
1. `CLAUDE.md` — 가장 중요. 이게 모든 결정의 기준.
2. `docs/ADR.md` — 어떤 결정이 이미 내려졌는지. 5개 시드 ADR 확인.
3. `docs/ARCHITECTURE.md` — 큰 그림과 데이터 흐름 ("플레이어 한 칸 움직이기"
   섹션이 핵심).
4. `docs/PRD.md` — 비어있어요. 본인이 채울 것.

모르는 용어 표시해두고, 다음 Step에서 질문 거리로 활용.

### Step 2: PRD 채우기 (30~60분, AI와 같이)

Claude 세션 시작하고:

```
docs/PRD.md를 같이 채우자. 나는 학부생이고, 친구들이랑 같이 할 수 있는
2D 사이드스크롤 MMORPG를 학습 목적으로 만들려고 해. 동시 접속은 ___명
정도 목표.
```

AI가 질문 던져가며 채워줄 거예요. 특히 **MVP 제외 사항**을 빡빡하게 잡으세요.

### Step 3: 첫 개념 학습 (15~30분)

```
/concept 권위 서버
```

이 개념이 우리 프로젝트의 토대라서 단단히 가는 게 좋아요. 이해되면:

```
/concept TCP
```

도 추천. 우리가 raw TCP로 가서 이 개념 깊이 알아야 해요.

### Step 4: 첫 마일스톤 계획 (10분)

```
/plan M1 - 서버 기본 골격 만들기. Hello World 수준에서 시작.
```

AI가 Phase로 쪼개서 `phases/M1-foundation/` 안에 파일들 만들어줄 거예요.
검토하고 필요하면 조정.

### Step 5: 첫 Phase 실행

```
phases/M1-foundation/01-XXX.md 부터 시작하자
```

AI가 그 Phase 파일을 읽고, 그 범위 안에서만 작업. 끝나면 5단계 보고.

### Step 6: 그날 멈출 때

`/review`로 최종 점검 후, "오늘 여기까지 한 거 정리해줘"로 마무리.

---

## 자주 막힐 때 + 학습 정리

| 상황 | 쓸 커맨드 |
|------|-----------|
| 이게 왜 필요한지 모르겠음 | `/why <대상>` |
| 코드 한 줄 한 줄 모르겠음 | `/explain <코드>` |
| 개념 자체를 처음부터 알고 싶음 | `/concept <키워드>` |
| 어디까지 했는지 잊음 | `/recap` |
| 방금 답변이 너무 어려움 | `/dumb-it-down` |
| 큰 목표를 어디서부터 시작할지 모름 | `/plan <목표>` |
| 코드 짜고 나서 점검하고 싶음 | `/review` |
| **Phase 끝남, 학습 정리하고 싶음** | `/journal-phase` |
| **개념 깊이 이해했음, 본인 말로 정리** | `/journal-concept <키워드>` |
| **막혔던 버그 풀었음, 잊기 전에 기록** | `/journal-bug` |

---

## 마음가짐

- 모르는 게 정상이에요. 이 프로젝트는 학부 수업 수준을 넘는 내용을 다뤄요.
- AI한테 "이게 왜?"라고 자주 물어보세요. 그게 안목을 키우는 가장 빠른 길이에요.
- 한 번에 100% 이해하려 하지 마세요. 60% 이해하고 다음 단계 가도, 다음 단계
  하면서 앞 단계 이해도가 올라가요.
- 막히면 멈추고 자고 일어나기. 진짜로 효과 있어요.
- **AI가 실수하면 → 마구를 고치세요.** 헌법, ADR, hook을 보강해서 같은
  실수가 구조적으로 못 일어나게. 이게 시간 지날수록 Harness가 똑똑해지는 비결.

---

준비됐으면 Claude 세션 시작하고 위의 Step 2부터 가보세요.
좋은 항해 되세요. 🚀
