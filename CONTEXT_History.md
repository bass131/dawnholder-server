# CONTEXT_History — 갱신 이력

> `CONTEXT.md`의 누적 섹션 외부화 (헌법: 문서 세분화 정책, Level 1).
>
> 본 파일이 220줄을 넘으면 주제별 카테고리(`CONTEXT_History/{topic}.md`)로
> 분리. 후보 카테고리: phases / policies / structure / meetings.

---

## 갱신 이력

| 날짜 | 변경 |
|------|------|
| 2026-05-06 | 미팅 결과 + ServerDev 시나리오 B 결정 + ADR-001/002/010/011 박제 |
| 2026-05-09 (5번째 세션) | Phase 01 완료 + ADR-001 v3 (Unity 6.4 LTS) + 응축 정책 박제 |
| 2026-05-09 (응축 재작성) | 200줄 내외로 처음부터 재작성. 옛 디테일은 git history + 노션 + learning-journal로 이관. START_HERE.md / harness-structure.html 삭제. |
| 2026-05-09 (폴더 prefix) | 모든 최상위 폴더에 정렬용 숫자 prefix 부여 (`00_Document/`, `01_Phases/`, `02_Server/`, `03_Client/`, `98_Shared/`, `99_Tools/`). `Dawnholder.slnx` + `global.json` 신설. .gitignore / csproj / 헌법 / 서브-CLAUDE.md 경로 정합성 일괄 갱신. |
| 2026-05-09 (Phase 02 재정의) | ServerCore 마이그 함정 실측 (nullable 13개뿐). 그 결과 *마이그 가능*하지만 *현업 표준 + 학습 가치*는 분리 모델 우세 판단. **Phase 02 = 서버측 ServerCore 정착(.NET 10 그대로)으로 축소. 클라측 socket 전략은 Phase 03에서 X/Y 결정.** Hook 보강도 Phase 02 후 사례 기반으로 미룸. |
| 2026-05-09 (Phase 02 완료) | ServerCore 7파일을 `02_Server/Network/`에 정착(.NET 10 유지, namespace `Dawnholder.Server.Network`). nullable 21곳 청소(실측 13 + .NET 10 추가 8). JobQueueTests 2개 추가. 빌드 경고 0 / 오류 0, 테스트 3 통과. commit `c2ea772`. 노션 세션 로그 박제. |
| 2026-05-10 (박제 정책) | 학습 일지가 밀릴 것을 전제로 **`-DONE.md` 페어 박제 정책 도입**. AI가 5단계 보고 직후 사실/결정/증상/키워드를 `01_Phases/{milestone}/{phase}-DONE.md`에 박고 commit. 학습 일지(본인 회고)와 역할 분리. 헌법 + `/journal:phase` 스킬 갱신, Phase 01·02 소급 작성. **Phase 03 갈래 = Y2(분리 + 별도 클라 라이브러리) 확정**, ADR + Phase 03 파일 재작성 직전. |
| 2026-05-10 (Phase 03 완료) | `04_ClientNet/` 신규 라이브러리 (Connector / ClientSession+PacketSession / Recv·SendBuffer / SmokeProbe). 5개 프로젝트 빌드 경고 0 / 오류 0. Unity F12 → 원본 한국어 주석 ReadOnly 표시 검증 통과 (ADR-010 두 번째 인스턴스). commit `fb7a06d` + `c3f2246`. 다음 = Phase 04 (Listener wire-up + connect 스모크). |
| 2026-05-10 (문서 세분화 정책) | 헌법에 **220줄 세분화 정책** 박음 (누적 섹션 외부화 / 응축 / 자르지 않음의 3분류 + 분리된 파일 재귀 세분화 시 주제별 카테고리 우선). 시범으로 `CONTEXT.md`의 갱신 이력을 본 파일(`CONTEXT_History.md`)로 외부화. |
| 2026-05-10 (헌법 응축) | 자기 정책 위반 해결 — `CLAUDE.md` 348→264줄. 사용자 컨텍스트 섹션 5개(톤·용어·결정 / 5단계 보고 템플릿 / -DONE.md 박제 / 학습 일지 권유 / 도구 카탈로그) 응축. `-DONE.md` 템플릿은 `.claude/templates/done-md-template.md`로 외부화. 절대 원칙·세분화 정책·구조 섹션은 본질이라 보존. commit `d01624c`. |
| 2026-05-10 (헌법 350줄 예외) | 헌법(`CLAUDE.md`)의 220줄 임계 자기참조 무한 제안 루프 차단. 헌법만 **350줄 임계**로 예외 처리(현재 264줄 → 86줄 성장 여유). 다른 사전형 문서(ARCHITECTURE.md 등)는 220 그대로 — 필요 시 별도 ADR. |
| 2026-05-10 (harness outdated 정정) | `.claude/` 18개 파일(서브에이전트 6 + 슬래시 커맨드 10 + hooks 2)에 박혀있던 옛 폴더 prefix(2026-05-09 정렬 변경 시 누락)를 sed로 일괄 정정. 추가로 Agent 점검 후 잔존 2건 정정 (log-session.md circular ref / done-md-template.md 참고 사례 mismatch). 핵심 통찰: 헌법 응축으로 인한 outdated = 0건. 폴더 prefix만 진짜 주범. commit `54838a5` + `15bbeb7`. |
| 2026-05-10 (Phase 04 완료) | 서버 `Program.cs` Listener wire-up(0.0.0.0:7777) + `GameSession.cs` 신작 + Unity 측 3파일(`MainThreadDispatcher` / `UnityClientSession` / `NetworkBootstrap`). 양쪽 OnConnected 로그 동시 + UnityException 없음 → main thread queue 패턴 작동 입증. ADR-012(Y2 분리) 분업이 *진짜 작동*함을 시연. SampleScene에 Network GameObject 박힘. Phase 04 파일은 이름 유지(framing 가정 outdated였지만 본문 재작성으로 정합). commit `a798479`. 다음 = Phase 05 (framing + Ping/Pong). |
| 2026-05-10 (전체 outdated 점검 + 정합) | Phase 05 진입 전 Explore agent로 `00_Document/` + Phase 파일 일괄 점검. 주요 발견: ARCHITECTURE.md가 2026-05-06/09/10 세 차례 변경(ADR-002 v2 / 폴더 prefix / ADR-012)에 *전부 누락*. 디렉토리 구조 diagram 통째 재작성 + MessagePack 의존성 제거 + EF Core 8→10 + Y2 socket 분리 모델 명시 + 변경 이력 placeholder 박제. PRD.md 1줄(`phases/` → `01_Phases/`) 정정. PRD.md 229줄(220 초과)은 별도 작업으로 미룸. Phase 05 파일 신설(`05-framing-ping.md`, BitConverter 임시 + Phase 06에서 PDL 이주 plan). commit `d5b8677` + `6494896`. |
| 2026-05-10 (Phase 05 완료 + ★ M1 Foundation 마일스톤 완료) | framing(`[size(2)][packetId(2)][payload]`) + 첫 Ping/Pong 양방향 시연. `98_Shared/Protocol/` 신설(PacketId enum + PingPacket + PongPacket, BinaryPrimitives.*LittleEndian 명시 + ToBytes/Read 페어). 서버 GameSession + Unity UnityClientSession 모두 PacketSession 상속으로 교체. Unity Update에서 1초마다 Ping → 서버 Pong → 클라 RTT 출력. 5개 프로젝트 빌드 경고 0/오류 0, 1분 안정성 통과. 직렬화는 BitConverter 임시(Phase 06에서 PDL 이주 예정). commit `5174573`. ★ M1 Foundation 완료 = 영상 시연 가능한 첫 데모. |
| 2026-05-10 (Phase 06 완료) | PacketGenerator 4월 ServerDev → `99_Tools/PacketGenerator/` 이주. **하드코딩 버그 2개 정정**: ADR-002 v2가 명시한 L178 `C_Chat`(자리 표시자 누락) + grep으로 발견한 추가 L194 `chatLen` → `count`. Program.cs nullable 정합(static string =""+`string?`+`return null`→throw). PDL.xml=`C_Ping`/`S_Pong` 두 패킷만(4월 게임용 통째 제거). csproj 정리(ApplicationIcon/PackageOutputPath 등 잔재 제거). 생성기 실행 → 3개 .cs(GenPackets 214줄+ClientPacketManager 79줄+ServerPacketManager 79줄) 정상 출력. 6개 프로젝트 빌드 경고 0/오류 0. 본 Phase 한계: 양쪽(서버+클라) 정합 + Phase 05 임시 BitConverter 코드 교체는 Phase 07로 분리. commit `03994b0`. |
| 2026-05-10 (Phase 07 완료 + ADR-012 진화) | PDL 정합 + Phase 05 임시 코드 교체 + Unity 시연 재현. PacketFormat.cs 템플릿 정정(Write byte[] 반환 + BinaryPrimitives.*LittleEndian + namespace=Shared.Protocol + ServerCore 의존 제거) + Program.cs 출력 폴더 분리(`98_Shared/Protocol/Generated/`로 패킷 통합) + `--no-manager`/`--no-wait` 옵션. Phase 05 임시 PingPacket/PongPacket/PacketId 삭제 → 생성 `C_Ping`/`S_Pong` 사용. GameSession/UnityClientSession/NetworkBootstrap dispatch 정합 (PacketID/camelCase/Write()). Unity 시연 통과 (Phase 05 RTT 로그 동일 재현). **사용자 통찰로 ADR-012 진화**: "전부 분리" → *책임 단위* (socket/buffer 분리, 패킷 정의 통합, manager/handler 분리). ADR-012 본문에 책임 단위 표 + 카테고리 맥락(Mirror/FishNet은 Unity 인디 카테고리, Rookiss=한국 MMO 백엔드 현업 표준) 보강. 책임 단위 문서화 6군데(ADR-012 + 98_Shared/CLAUDE.md + SendBuffer 양쪽 주석 + PacketFormat 헤더 + Phase 07 -DONE + 학습 일지 후보 키워드). **AI sliding 패턴 메모리화**: 결정 떠넘김+추천 흔들기 패턴을 사용자 멘토링으로 짚어줌, `feedback_decision_leadership.md` 박음. |
| 2026-05-11 (ADR-017 박제 + 한글 경로 영구 해결 + 정리) | 폴더 ASCII 이동(`C:\Dev\ClaudeDev`) 검증 6단 통과 → ADR-017 박음. WDAC error 4551(Burst Enable 시)은 별도 사건으로 ADR 범위 밖 명시, Burst Disable 유지 + 보류 항목(복잡도 시스템 도입 시점에 정리). 잔여물 정리: 옛 한글 경로 폴더 + `C:\Dev\ClaudeDev\ClaudeDev` 중첩 빈 잔여물(Move-Item 부산물) + Unity `_Recovery/` 자동 생성물. PacketGenerator manager 두 파일이 `using Shared.Protocol;` 누락으로 빌드 깨뜨려 삭제 + 보류 항목으로 박음(ADR-002 트레이드오프 3번째 잠복 버그, M1 Phase 06 정정한 2건과 같은 부류). 다음 = Phase 05(client prediction + snap reconcile) 진입 직전. commit `53a476c` + 본 사이드 트랙 박제 후속 commit. push 완료(12 commits 일괄). |
| 2026-05-11 (세션 마감) | Phase 07 누락 보강 — C_Ping/S_Pong 라운드트립 회귀 안전망 추가 (commit `2b1cc4d`). M1 Foundation 영역 테스트 커버리지 = Ping/Pong 한 군데. CONTEXT.md 정리: stale "다음 Phase = Phase 05" 섹션을 "다음 세션 결정 사항 (Phase 08 vs M2 진입)"으로 교체, 추천 = M2 진입 (사유: 추상화는 패킷 5~10개에서 진짜 형태 보임 + M2가 캡스톤 1 데모 가치 큼). 사이드 작업 후보: PRD.md 응축(229줄) / 학습 일지(Phase 02~07 누적). |
| 2026-05-11 (문서 협업 시스템 정합 + Notion e2e 검증) | 8 commit (`6333022..64a1afa`). ① 일회성 HTML 3건 정리. ② Notion 협업 분업 원칙(Claude=사실 박제 / Codex=Notion 재편집 / 본인=회고)을 CONTEXT 응축 위험에서 보호하려 `.claude/templates/done-md-template.md`로 영속 이주(CONTEXT엔 한 줄 참조만). ③ Claude→Codex 핸드오프 절차 박음 — 트리거(사용자 신호) / 입력 셋(-DONE.md + CONTEXT* + ADR + 템플릿 + log-session 명세) / 책임 분담(Claude=Bash로 Codex 호출, Codex=Notion 작성, 본인=검토) / Codex CLI cmd 형식 (create는 `-s workspace-write`, update는 `--dangerously-bypass-approvals-and-sandbox`, stdin은 `< /dev/null`). ④ Notion 출력 형식 모순 해소(STAR=출력 표준 / 8단=사고 체크리스트, 매핑 표 양쪽에 박음). ⑤ -DONE.md 템플릿에 TL;DR 섹션 + Phase 07 -DONE 소급 적용. ⑥ `/session:log` deprecation → Codex 환경 명세로 reposition. **e2e 검증 결과**: dry run → 사용자 검토 → 실제 박기. 노션 1 신규 페이지(오늘 세션) + 기존 9 페이지 중 7개에 34개 용어 풀이 소급 보강(본문 무변경 검증 OK, 첫 시도는 stdin hang + update MCP confirmation 거부 함정 → 재시도 후 성공). |
| 2026-05-11 (Post-flight 게이트 도입) | `jha0313/harness_framework` 비교 후 자동 실행 루프는 비채택(학습 호흡 보존), 대신 박제 시 빼먹기 방지를 훅으로 강제. 새 훅 `.claude/hooks/validate-phase-gate.sh` + 템플릿 frontmatter(`summary` 1줄/`phase`/`status`) + `## AC 검증 결과` 섹션 필수 + 5단계 보고 5개 항목 라벨 검사 + `settings.json` 훅 등록 + 헌법에 Post-flight 게이트 절. 누락 시 `exit 2` 차단. 게이트 ①(Pre-flight)·③(Blocked 명시)은 ② 실전 1회 검증 후 결정. **하네스 비대화 우려 표명** — 향후는 *추가*보다 *정리* 방향 합의(슬래시 14개/에이전트 6개/문서 시스템 실사용 빈도 기반 가지치기 후보, M2 진입 시점에 응축 같이 가는 안). |
| 2026-05-11 (M2 진입 전 도구대 정합) | M1 Foundation 완료 시점 사이드 정비 일괄 처리. ① **PRD.md 응축** 230→151줄 (위험/캡스톤/MVP 압축, 220줄 임계 위반 해소, Phase 06부터 미뤄진 항목). ② **ADR 전체 점검 + 갱신** — outdated 4건 정합 (ADR-002 폴더 prefix / ADR-005 EF Core 8→10 / ADR-010 폴더 prefix / ADR-011 PacketGenerator 버그 후속) + 신규 ADR-013~016 박음 (-DONE.md 페어 박제 정책 / 220줄 + 헌법 350줄 예외 / Post-flight 게이트 / Notion 3자 분업) + 변경 이력 `00_Document/ADR_History.md`로 외부화 (CONTEXT_History 패턴 재사용). ADR.md 204줄 (220 임계 16줄 여유). ③ **CONTEXT cross-ref 박음** (ADR-013/014/015/016). ④ **CONTEXT.md 응축** 225→본 응축본 (Phase 01~07 디테일은 본 History + -DONE.md/노션으로 위임, 큰 마일스톤 끝날 때마다 재작성 정책 부합). 다음 = M2 First Connection 진입 (`/work:plan`으로 분해 직전, 옵션 B 채택). |
| 2026-05-11 (M2 Phase 05 완료) | Client-side prediction + snap reconcile 도입. `PlayerPredictor.cs`(순수 C# 클래스, 미래 EditMode 테스트 가능성 보존) / `LocalPlayerController` 통합(매 frame Predict → transform 동기, `OnServerSnapshot` 진입점) / `UnityClientSession.SimulatedLatencyMs` Editor only + `SendIntent` 래퍼(decorator 패턴, A/B/C 7축 비교 HTML로 선택) / `MainThreadDispatcher.EnqueueDelayed`(timestamp 큐 + lock). 4/4 완료조건 모두 wire 데이터로 검증 — ① 즉시 반응 / ② latency 0 분당 ~2회(SnapThreshold 0.5→1.0 튜닝 후) / ③ latency 200 분당 ~26회, dx 1.0~2.0, **좌우 전환 시 클러스터링(본인 시각 관찰, prediction 본질 한계 발견)** / ④ cheat 시뮬 `dx=-1000.00` → ~450ms 안 원위치(**헌법 #1이 코드로 작동하는 순간이 로그에 박힘**). 부수: Phase 03·04 라운드트립 회귀 가드 +38 케이스(`PacketRoundTripTests` test 25→63), **240Hz framerate-bound 송신 발견**(300-500/s, Phase 04 본 리뷰 🟡 실측 노출) → rate-limit 100→500 + 첫1회만 로깅(임시방편, 본질 fix는 Phase 06 fixed timestep), PDL 주석 outdated 정정. commit `b02df49`(본체) + `a118552`(DONE.md hash 정정). 노션 박제(STAR + 용어 풀이). 다음 = Phase 06 (input replay reconcile — snap 대신 last-ack 이후 입력 재실행으로 부드럽게). |
| 2026-05-12 (ADR-018 박제 — 하네스 망각 안전망) | AI 컨텍스트 디케이 진단 → 입구·출구 한 짝 안전망 + WORK-ID 합류 마커링 도입. Codex CLI 3라운드 자문(R1 표준 봉투화 / R2 LLM self-check 금지 / R3 WORK-ID·핀 갱신 정책) 교차 검증. 6파일 신규(`.claude/state/current-pin.txt` / `inject-current-pin.sh` UserPromptSubmit / `check-work-envelope.sh` Stop / `pin-template.txt` / CLAUDE.md 봉투·핀 섹션 / settings.json 훅 등록) + 정합 갱신 5파일(CONTEXT.md / done-md-template.md / work:plan 4.5 단계 / learning-journal 3 템플릿). 후속 트랙(SessionStart 훅 / 라우팅 결정 트리)은 페인 분류 A(단순 반복)/B(의지 표현)/C(안전·확인 게이트) 재정렬 결과 B형으로 비채택. commit `08e7ca3` + 정합 `4f86cc1`. |
| 2026-05-14 (ADR-018 사후 검증 + ADR.md 외부화 시나리오 결정) | 5/12 박은 5개 훅 동작 여부를 48시간 만에 검증. 1차 stderr 마커 → Claude Code가 PostToolUse/Stop exit 0 stderr를 silent 처리한다는 가설 확인. 2차 파일 append 트레이스(`.claude/state/hook-trace.log`) → PostToolUse 3개 + Stop + UserPromptSubmit 5/5 정상 박힘 확정. PATH 정정 — 시스템 환경변수에 `C:\Program Files\Git\bin` 추가(Git for Windows 정식 설치는 이미 되어있었음, PATH만 빠져있었음). 검증 자산 정리 완료. **ADR-020(훅 실행 환경 의존성) 박제 시도 → ADR.md 220줄 임계 초과 발견 + 후보 표 번호 충돌**(후보 ADR-020 = 캐릭터 데이터 스키마). 시나리오 A 채택: (1) ADR.md를 헌법 ADR-014 정책 (b) 카테고리 분리 패턴(`00_Document/ADR/{tech-stack, gameplay, harness}/`)으로 재편 → (2) ADR-020 박제 → (3) 후보 표 번호 재배치(ADR-020 → ADR-021). 부수 발견: Windows에서 bash 훅 검증 시 파일 append 트레이스 패턴(`echo "[HOOK-MARKER] ..." >> log`)이 재사용 가능 도구. ADR-020 부록 A로 박힐 예정. |
