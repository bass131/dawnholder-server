# Codex β 프롬프트 — M4.5 Phase 04 양방향 전투 + v9 bump (2026-06-07)

아래 블록을 Codex 세션에 그대로 붙여넣기.

---

너는 2D 사이드스크롤 MMORPG(.NET 10 권위 서버 + Unity 클라) 코드의 외부 리뷰어다. 마일스톤 마감 전 cross-check 임무. 저장소는 현재 디렉토리(C:\Dev\ClaudeDev)다. **읽기 명령은 허용** — `git show ef0082d`, `git diff ef0082d^1..ef0082d`, `git show 897a90d`, 파일 열람 자유. 단 **파일 수정/생성/삭제, 네트워크 접근, web search는 금지** (read-only 리뷰).

## 점검 대상

1. git commit `ef0082dfd73e212c2f501ce638f98bbbc4a88e50` (PR #71 머지 — 주 대상): 보스 전투 서버 FSM + 프로토콜 v9 bump 한 묶음
2. 현 브랜치 HEAD 후속 commit `897a90d`: ComputeBossAnimState 우선순위 Death > Attack > Hit 변경 (보스 한정) + 테스트 2개

핵심 파일:
- 02_Server/GameServer/Maps/Systems/BossBehaviorSystem.cs (FSM: 쿨다운 40/24틱 → telegraph 16/10틱 → AABB 판정 → 사망/리스폰)
- 02_Server/GameServer/Combat/CombatConstants.cs, EnemyEntity.cs
- 98_Shared/Protocol/Generated/GenPackets.cs + 99_Tools/PacketGenerator/PDL.xml (S_EnemyAttack ID 20 신설, S_PlayerJoin.characterClass byte append, ProtocolVersion 8→9)
- 03_Client/Assets/Scripts/Network/ClientPacketHandlers.cs (EnemyAttackHandler — 클라 소비 측)
- 99_Tools/headless-bot/Scenarios/BossFightSmoke.cs

## 프로젝트 절대 원칙 (위반 = 즉시 보고)

1. 서버 권위 — 클라는 렌더러+입력 전달자. 데미지/판정/HP는 서버 전용
2. 프로토콜 신성 — 패킷 ID 재사용 금지, 필드는 append-only, wire 변경 시 ProtocolVersion bump
3. 신뢰 경계 — 클라 소켓 입력은 전부 untrusted (범위/소유권/rate-limit 검증)
4. 공유 코드 규율 — 98_Shared 변경은 양쪽 컴파일 확인
5. 틱 루프(20 TPS) 블로킹 금지 — await Task.Delay / 동기 DB / Thread.Sleep / 매 틱 할당 금지

## 중점 점검 가닥

- 보스 FSM 엣지: 페이즈2 전환이 telegraph 도중 발생 시 카운터 정합 / 보스 사망 틱과 telegraph 겹침 / 대상 플레이어 전원 사망·이탈 시 FSM 진행
- ApplyBossAttack의 사망 처리: S_EnemyAttack에 실리는 HP 값 순서(리셋 전/후), foreach 중 컬렉션 mutation 위험
- PDL append-only + GenPackets 재생성 drift (PDL.xml과 생성 코드 불일치 여부)
- S_EnemyAttack 직렬화 왕복 — 음수 HP(int), attackPattern(byte) 경계값
- 897a90d 우선순위 변경이 보스 외 경로(EnemyAISystem의 일반 적)로 새는지 — 보스 한정이어야 함
- 부정 조건 분기(!= X) 함정 / 하드코딩 잔존 / 문서 주석 약속 vs 실제 코드 격차

## 출력 형식 (총 40줄 이내 — 초과 금지)

- 발견 결함: `[P0~P3] 파일:라인 — 한 줄 설명 + (확신/추측/불명 자가 표기)` 형식, 심각도순
- 결함 0이면 "결함 0" 명시 + 통과 근거 3줄
- 마지막 줄: GO / 조건부 GO / NO-GO 판정 한 줄

---

(참고 — α 리뷰는 🔴0/🟡1[GenPackets Write 64KB 할당, 생성기 전역 패턴] 판정. 회귀 실측: WSL2 클린빌드 0/0, test 419/0/4, 봇 7종 PASS)
