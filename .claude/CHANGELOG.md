# CHANGELOG — 하네스 변경 이력

> **이 파일의 역할**: 팀장(유영호)이 헌법/ADR/하네스/공유 파일을 변경할 때마다
> 한 줄씩 박제. 팀원이 매일 작업 시작 시 `/session:start`가 이 파일의 최근 줄을
> 자동으로 보여줘서 본인이 옛 결정 기반으로 작업하는 사고 예방.
>
> **갱신 주체**: 팀장(유영호) 단독. 팀원은 읽기만.
>
> **갱신 규칙**:
> - 변경 commit 직후 한 줄 추가
> - 형식: `YYYY-MM-DD — 한 줄 요약 (영향 범위 / 위험도)`
> - 위험도 표기: `[L]` (저위험, 추가만) / `[M]` (중간, 행동 변경) / `[H]` (고위험, 결정 뒤집기)
> - 슬랙(또는 디스코드)에도 같은 한 줄 알림 박기

---

## 갱신 룰 짧게

**저위험 [L]** — 새 슬래시 커맨드, 새 서브에이전트, 새 ADR 박제 (기존 결정 변경 X), 학습 일지 템플릿 보강
**중간 [M]** — 슬래시 커맨드 동작 변경, 기존 ADR 사후 보강, 헌법 일부 섹션 추가
**고위험 [H]** — 헌법 절대 원칙 수정, 기존 ADR 뒤집기 (PostgreSQL→MSSQL 같은 결정 정정), 파일 권한 분리, 영역 분리 변경

[H]는 팀장이 슬랙/디스코드에 추가 안내 + 팀원 작업 일시 중단 요청 권장.

---

## 이력 (최신이 위)

| 날짜 | 변경 | 위험도 |
|------|------|--------|
| 2026-06-07 | **M4.5 Phase 01+02 머지 (PR #68/#69) — 적 시각 prefab 전환 + 골렘 추가 + EnemyKind 98_Shared 이사** — **Phase 01(PR #68)**: `EnemyViewFactory.BuildPlaceholder`(149줄 런타임 조립) 은퇴 → `EnemyVisualTable` SO lookup + prefab Instantiate. 적 prefab = **Slime/Boss_Vampire**(본인 제작 아트로 통일) — 외부 placeholder **Mushroom/ToxicFrog 113파일 삭제**(guid 참조 0 전수 확인). 새 적 추가 = "prefab 1개 + 테이블 1행". `InternalsVisibleTo` 첫 도입(테스트 전용 internal 노출). **Phase 02(PR #69)**: 골렘(EnemyKind=2) — GolemDefault(HP60/방어5/속도1.2) + HG 마커 + 재bake(Town/BR 무변경). **EnemyKind 서버/클라 중복 정의 → `98_Shared/GameData/EnemyKind.cs` 단일 정의 이사** (헌법 #4 봉합). 화석 분기 3곳 정정(`!= Normal` → `== Boss` — "적 2종" 가정 깨짐, 4종째부터 코드 변경 0). **pull 시 영향**: ① Shared.dll 자동 갱신(EnemyKind 위치 이동 — 서버 코드에서 `Dawnholder.Server...Combat.EnemyKind` 참조 시 컴파일 에러 → `Shared.GameData` using으로) ② Mushroom/ToxicFrog 폴더 삭제 반영(참조 0이라 Missing 없음) ③ HG 씬 갱신 — HG 저작 전 pull 필수. **ProtocolVersion 8 불변** (kind = byte cast, 재빌드는 Shared.dll 복사로 충분). 검증 = dotnet 399/0/4 + EditMode 63/63 + 봇 PASS + Play 실측 + reviewer 🔴0 ×2회. | [M] |
| 2026-06-07 | **M4.4 World & Player 마일스톤 완전 마감 (Phase 06 회귀 + PR #67)** — **one-way 발판 저작**: `PlatformPlank` Tile 신설(Cainos sprite, Art/Tiles/Platform/) + HuntingGround 5발판 + BossRoom 6발판(섬 보물상자 도달 가능화) + 재bake(Town 무변경 idempotent 3회째). 두 맵 공중 지형은 발판 전까지 전부 도달 불가였음(진입 단차 +2~5 vs 최대 점프 1.6u — 발판은 출발면 +1 계단식 저작 스펙, 하향 점프는 지형 v2 이월). **Phase 06 회귀 풀세트** (WSL2 = ADR-029 표준 경로 첫 실전): 클린빌드 0/0 + test **392/0/4 = CI 동일** + 봇 5종 PASS(desync 0.00) + Play 실측 직업 2종×세 씬 매트릭스 이상 무 + **ProtocolVersion 8 불변 — 전 마일스톤 bump 0 약속 이행** (다음 bump = M4.5-04 8→9 유일). 마일스톤 박제 = `_milestone-DONE.md`+`.html` 5단계 보고(대규모). **pull 시 영향**: HG/BR 씬에 발판 자산 + terrain.bin 갱신 — 두 씬 저작 작업 전 pull 필수(충돌 예방), Shared.dll 재빌드 불필요(v8 유지). 다음 = M4.5 content-and-boss 정식 분해(유현 UI 문서 입수 후 `/work:plan`). 몬스터/보스 외관 디테일 = M4.5-01/04 흡수. | [M] |
| 2026-06-07 | **ADR-029 채택 — SAC dotnet 실행 차단 → WSL2 실행 표준 (로컬 테스트 부활)** — SAC(Smart App Control)가 세션16 dotnet test 차단에 이어 풀 리빌드 후 **서버 실행까지 차단** (fresh unsigned dll CoreCLR 로드 차단, 평판=파일 바이트 단위, 차단 대상 비결정 "빌드 룰렛" — entry dll 통과/Shared.dll 차단 실측). 사용자 결정(WSL2/SAC Off/신뢰바이트 도구화 3안 의논)으로 **WSL2 Ubuntu = 로컬 dotnet 실행(서버/봇/dotnet test) 표준 경로** 채택 — Windows `dotnet build`는 유지(ADR-010 Unity Plugins DLL 공급), CI(ubuntu)는 원격 안전망 그대로. PoC 5항목 게이트 통과: 빌드 5.68s / 서버 기동 / 봇 desync 0 / **test 392-0-4 = CI 동일 (세션16 포기했던 로컬 테스트 부활)** / Windows→wslrelay TCP 검증 + Unity Play 실측. 세션16 "SAC 게이트 은퇴 = CI 단독 안전망" 부분 supersede. 표준 명령 모음 = ADR-029 부록 A. **SAC 켜진 머신만 해당** — SAC 없는 머신은 기존 그대로. LocalDB Linux 부재는 M5 영속화 진입 시 결정 이월. | [M] |
| 2026-06-06 | **M4.4 Phase 03~05 머지 (PR #64/#65/#66) + 유현 합류분 (PR #61) + GitHub Actions CI 신설** — **Phase 03** 맵 데이터 바이너리 파이프라인: 생성 C# 코드젠 은퇴 → `terrain.bin`(공유)/`content.bin`(서버 전용, 헌법 #1) + DWMP·CRC32 무결성 헤더 fail-closed + 런타임 로드 + 스폰/kill-plane bake (`MapSpawnTable` 하드코딩 은퇴). **Phase 04** 직업 이동 분리: `MoveParams` 필수 인자로 Physics 주입 + `Constants.MoveSpeed`/`JumpSpeed` 은퇴 (전 직업 5.0 죽은 값 → Warrior 4/Ranger 6 실연결 — *체감 속도 변화는 의도된 정정*) + LocalPlayerController 4책임 분할. **Phase 05** 직업 장착: `ClassConfig` SO(Resources/ClassConfigs) lookup으로 Animator+공격 전략 데이터 장착(조작 코드 직업 분기 0) + `PlayerStats.ForClass` 단일 출처 + 로컬 애니 hybrid(이동=로컬 예측/전투 이벤트=서버 animState) + Town EventSystem 중복 제거(UI 씬 단일 출처). **PR #61(유현)** UI 핸드오프 문서 + UI 아트 + BgmComposer 도구(99_Tools). **CI 신설 = 모든 팀원 영향**: `.github/workflows/dotnet-tests.yml` — `feature/**` push + PR마다 dotnet build+test 자동 (ubuntu, 로컬 SAC 차단 무관 안전망). pull 받으면 자동 적용 — 본인 PR에 빨간 X 뜨면 테스트 실패이니 머지 전 확인. 테스트 392/0/4 유지, **ProtocolVersion 8 불변** (Shared.dll 재빌드 불필요). | [M] |
| 2026-06-06 | **M4.4 Phase 01+02 머지 (PR #63) — 타일맵 레이어 약속 + 지형 bake 도구 + 서버 권위 지형 물리** — **Phase 01** `TerrainBaker` 에디터 도구(Tools/Dawnholder/Bake Terrain): 씬 타일맵 → 98_Shared 생성 지형 데이터 (Town/HuntingGround/BossRoom, AABB 24개, `7fd8dc0`). **레이어 약속 신설 = 모든 씬 저작자 의무**: `Tilemap_Solid`(바닥·벽) / `Tilemap_Platform`(one-way 발판 — 발판은 반드시 Platform 레이어에 그리기), 그 외 이름 장식 레이어 자유. **씬 수정 → bake 재실행 → 산출물 동반 commit 의무** (PacketGenerator drift 관례 동일). **Phase 02** `Physics.StepWithTerrain`(`c5e615e`): 솔리드 AABB 충돌(축 분리 + 교차 판정 tunneling 방지) + one-way 발판(아래서 점프 통과/위 착지) — 점 모델, 헌법 #5 틱 무할당. 기존 호출부 12파일 무변경(오버로드), StepFlat fallback float 완전동일. 검수 결함 1건 봉합(벽 모서리 점프 등호 케이스). 테스트 381/0/4skip + cross-review γ 10회차 GO(α 🔴0 + β P0~P2 0). **Phase 03 진행 중 = 맵 데이터 바이너리 파이프라인 전환** (세션13 사용자 결정 — 생성 C# → terrain/content.bin 무결성 헤더 + 런타임 로드 + 스폰/kill-plane bake, `MapSpawnTable` 하드코딩 은퇴 예정, 상세 = Phase 03 문서 D1~D6). ProtocolVersion 8 불변. 모든 씬 저작자 영향(레이어 약속 + bake 동반 commit 의무) → [M]. | [M] |
| 2026-06-06 | **M4.3 ai-and-polish 경량 마감 (Phase 08b/10/10b/11/12) + 마일스톤 재편 (M4.4/M4.5 분리)** — **Phase 08b** 클라 애니 구조: `IMotionState` + `AnimatorDriver` — 서버 권위 `AnimState`(v8)를 클라 Animator int 파라미터로 구동하는 계약 신설 (`c22c6d3`). **Phase 10/10b** 이동감: 서버 입력 큐 one-per-tick FIFO로 rubber-band 근본 해결 (`8ad8840`) + 착지 직전 점프 보관 jump buffer로 점프 snap 봉합 (`324dfb3`). **Phase 11** 애니 외관 완성 (복잡): 본인 제작 5종(Knight/Mage/Slime/Golem/Boss_Vampire) 클립·상태 + 미술자산 머지(PR #58/#59/#60 + `b4ff1b2` 221파일 — 폴더 재배치 R100 guid 보존, 의도된 Missing 2건 박제) + **Animator 계약 wiring 5종** (`2940f2f` — AnimState 파라미터 + Any State/directed 전이 + Loop off 9건 + Knight `AttackVariant` 0/1 랜덤[시각 전용, 판정은 서버 권위] + 2단 체인은 directed 진입=Any State 핑퐁 방지). ⚠️ prefab 연결+Play 실측은 M4.4-05 흡수 (의도 — Art prefab 단독 조작 불가, -DONE 명시). **마일스톤 재편** (`958de75`, plan-auditor 조건부 GO 봉합 완료): 지형(언덕·단차·공중)+직업 분리 수요가 M4.3 입자 초과 → M4.3는 경량 마감(09 보스는 M4.5 이월), **M4.4 world-and-player**(타일맵 충돌 bake→98_Shared 단일 진실 + 직업 조작 분리) 신설 + M4.5 content-and-boss 스케치. **Phase 12 회귀**: 클린빌드 경고0/오류0 + 테스트 **349통과/0실패/4skip**(기존 장기 통합) + 헤드리스 봇 **6 시나리오 전부 PASS**(smoke/M2/MultiRoster/EmergencyCombat/BossStageClear/EnemyAi) + **ProtocolVersion 8 불변 확인**(M4.4 전체 bump 0 예정, 다음 bump는 M4.5-04 8→9 유일). 모든 팀원 영향(애니 계약 구조 + 마일스톤 좌표 변경 + pull 시 자산 대량 + Shared.dll 재빌드 불필요[v8 유지]) → [M]. | [M] |
| 2026-05-30 | **M4.3 Phase 08a(애니 프로토콜+서버) + M4.3X 코드 정리 마감** — **08a** (복잡): `AnimState` enum 신설 + `S_Snapshot`/`S_EntityState`에 `animState` byte append + **ProtocolVersion 7→8 bump** + 서버 권위 상태결정(player vx/grounded/이벤트, enemy AI state/이벤트, latch 8틱 헌법#5) + AnimStateTests 17 (`47b59d5`). **M4.3X 코드 정리** (복잡): CODE_CONVENTION **§6 주석 정책 신설**(self-documenting — 금지 5종[자명 재진술/역사 Phase 박제/폐기 사고과정/backlog/internal XMLdoc] + 안전 예외 5%[보안·프로토콜·헌법 함정]) + **§2.4 네트워크 세션 프레이밍 깊이 2 예외**(Rookiss framing) + 제목 v3→v5 / REVIEW_CHECKLIST 5축→6축 stale 정정. **Codex read-only 감사 갭 4종**: PacketGenerator §3.3 prefix(`_r`/`m_MakeFunc`/`_Segment`) 정합+재생성 / SceneTransition `_fadeGroup`·`_fadeDuration` rename+`FormerlySerializedAs` / 상속 §2.4 예외 / 문서 stale. **workflow 7도메인 → 124 .cs §6 전수 정리**(−2313/+949 삭제 위주, **동작 0변경**) — reviewer 🔴0(안전 주석 보존) + build 0/test 338 green + Unity Play 실측(맵전환3/에러0) + GenPackets drift 봉합 (`6d70a62`). **M4.3X 재범위화**: God class는 M4.3R(PR #56) 이미 완료 → plan 04/05 삭제·대규모→복잡 (옛 ADR-028 부록 A 줄 수=리팩토링 전 스냅샷). ⚠️ **§5 강제(reviewer §6 축 + SubAgent §6 규칙)는 `.claude` 스테이징→본인 적용 미완** (다음 세션 처리). 모든 코드 작업 영향(§6 주석 정책 = 코드 작성 방식 변경) + PDL v8(pull 시 Shared.dll 재빌드) → [M]. | [M] |
| 2026-05-29 | **Code Convention 수립 + 강제 §5 (ADR-028)** — M4.3 Phase 07에서 `GameMap` God class(665줄: 전투+AI+respawn+broadcast)가 드러났는데 reviewer가 못 잡음 (REVIEW_CHECKLIST가 "메서드/파일 길이=코드 크기=도구 책임"으로 *명시 제외*, SOLID는 🎓로만 강제력 0). 사용자 핵심 = "ADR·문서 선언만으론 안 지켜진다(신뢰 문제)" + "기반 애매하면 후속 코드가 다 부채" → **결정적 규칙 + 자동 강제 + 완성형** 지향. **두 권위서 정독**(워크플로우 32 agent/118만 토큰): Game Programming Patterns 19패턴 + 게임 서버 프로그래밍 교과서(배현직) 10장을 *각 1파일*로 색인화(`00_Document/conventions/refs/` + 폴더별 `_index`). **우리 규칙** = `CODE_CONVENTION.md` v3 (★§2.2 God class 분리 결정 기준: **2+ 도메인이면** 컨테이너[상태+tick 엔진]+System[로직] 분리, 과분할 경계 명시 / §0.3 과한 추상화도 부채 / §1 Map=Actor·콘텐츠-엔진 분리) + `INDEX.md` 작업유형 라우팅. **강제 4중** = (1) server/client/shared agent에 "코드 작성 전 INDEX 참조 의무" (2) **`REVIEW_CHECKLIST` 축 6 신설** — God class·패턴 위반·콘텐츠/엔진 혼재 점검, *기존 "코드 크기=도구 책임" 제외 조항을 구조/SRP에 한해 뒤집음* (ADR-019 부분 supersede) (3) `convention-size-guard.sh` hook (핵심파일 600줄+ 경고) (4) ADR-028 박제. **`.editorconfig`+Roslyn(순수 포매팅)은 M4.4 이월**. 이제 모든 코드 작업(팀원·AI)이 코드 작성 전 INDEX 참조 + God class §2.2 적용 의무 → **[H]**. commit `3afc528`(본체 33파일) + `db31fda`(강제 §5). M4.3 발표 Phase 08~12는 보류(기반 우선, 사용자 결정). 합류자(인규) 셋업 시 자동 인지. | [H] |
| 2026-05-28 | **`/cross-review` 슬래시 명세 γ 9회차 학습 갱신 (M4.2 마감 후속)** — M4.2 γ 9회차 결과(β 1차 봉합 → β 2차 추가 4건 + 사용자 통찰 "서버 구동 안 하고?" + 실측 검증 → β P1 false positive 가능성↑ / β P2 진짜 결함 실증)를 슬래시 명세에 정합 박음. **6 항목 갱신**: (1) 분담 정신에 Claude 직접 호출 OK 시점 추가 (`codex exec --sandbox read-only --cd "$PROJECT_DIR"` + prompt 가드 — 짧은 메타 질문/단일 점검 한정, 큰 cross-review는 본인 분담 default 유지) / (2) Step 4-A 봉합 후 재실측 의무 신설 (β 1차 봉합이 새 결함 차원 도입 가능, β 2차 재실측 의무 — M3.7 옵션 C 게이트 5번째 실측 패턴 정합) / (3) Step 4-B 실측 검증 의무 신설 (α/β 정적 시각 vs 실측 truth 격차, 사용자 Play 모드 실측이 final arbiter) / (4) Hard rules 7~9 추가 (재실측 의무 / 실측 우선 = β 신뢰 맹목 X / 위험 옵션 `--dangerously-bypass-...` 절대 금지) / (5) 함정 3건 추가 (정적 추론 vs 실측 truth 격차 / 봉합이 새 결함 도입 / Claude 직접 호출 truncation) / (6) γ 학습 정합 절에 γ 8회차 + 9회차 박제 + 후속 정신 명시. ★★★ 학습 자산 3건 박힘(`static-analysis-vs-empirical-truth-gap` + `cross-review-followup-finding-pattern` + `user-as-final-truth-arbiter`). 모든 팀원 영향 (다음 `/cross-review` 호출 시점) → [M]. 합류자(인규) 6월 말 셋업 시 새 분담 정신 자동 인지. | [M] |
| 2026-05-28 | **M4.2 Map Transition 마일스톤 완전 마감 (5 Phase 풀세트, 복잡 등급)** — M3 응급 단일 맵 3-zone trick을 진짜 4맵 분리로 승격. **Phase 01** (보통) 맵 레지스트리 + MapId enum 골격 + MapSpawnTable 단일 진실 (`dad760b`). **Phase 02** (복잡, irreversible 깃발) PortalTable + `C_EnterPortal`/`S_MapTransition` PDL append-only + **ProtocolVersion 5→6 bump** + Shared.dll 동반 commit (`8363425`). **Phase 03** (복잡, trust-boundary) 맵 간 player migration `SubmitEnterPortal` 4단 검증 + race window 4종(transient drop / disconnect during migration / GetDestMap null / broadcast skip) 결정론 봉합 + **AI 조율자 동시성 설계 케이스 스터디 HTML** 박제 (`41f224d` + `8c11e91`). **Phase 04** (복잡, unity-asset) ADR-027 Client Bootstrap Persistent Services + 동적 spawn + Play 검증 봉합 3건(카메라 미추적 / spawn 튐 d=2 / 첫 snapshot race — "위치 아니라 생명주기" 재정의) (`a59cb1b` + `cc532d5` + `8ccedd8`). **Phase 05** (보통, qa) 봇 portal 이동 흐름(`EmergencyCombatSmoke`/`BossStageClearSmoke` 봉합) + 새 `MapTransitionScenario` 4맵 루프 + 통합 테스트 + 옛 Skip smoke 2건 복구 + ARCHITECTURE.md `## M4.2 결과 종합` 절 신설 + L212 본문 정정. **ADR 2건 신설**: ADR-026(entity id 전역 풀 — `S_MapTransition`에 entityId 필드 *없음* trade-off, 한국 게임 회사 패턴) + ADR-027(코드 주도 Bootstrap + Persistent Services + A안 연결 생명주기). **테스트**: 300통과 / 0실패 / 4Skip (M4.1 baseline 221 → +79). **reviewer Tier 2-A 통합 점검 통과** (🔴0 / 🟡3 — 🟡 1·2 본 commit 동반 봉합 / 🟡 3 봇 portal const 공유 헬퍼 M4.3 backlog). **false-promise 점검 결과** (ADR-024 cadence — 마일스톤 마감 의무): 누적 1건 발본 (역방향 ARCHITECTURE.md stale, 본 commit 봉합 — M4.1 동일 패턴 재발 예방 정합). forward 약속 0건 — plan 명시 풀세트 모두 실현. **M4.3 이월 명시**: cheat-flag table / Serilog / 맵 간 enemy respawn 정책 / 봇 portal const 공유 헬퍼 / reconcile drift 튜닝 / `GameSession/GameMap` spawn 종속성 분리. **★★★ 학습 자산 5건 박힘**: `entity-id-global-pool-not-in-packet` / `map-migration-race-deterministic-coverage` / `lifecycle-not-position` / `client-bootstrap-explicit-lifecycle` / `reverse-false-promise-architecture-stale`. **캡스톤 1 발표 데모 영상 가능 상태** (M4.1 정밀 전투 + M4.2 4맵 분리 종합). 모든 팀원 영향 (ProtocolVersion 5→6 bump → 다음 pull 시 Shared.dll 재빌드 + 새 PDL 인지 의무) → [M]. | [M] |
| 2026-05-24 | **M4.1 Phase 06 마감 (lag compensation 200ms rewind + AABB hitbox + B1/B3 sweep, 복잡 등급) → M4.1 마일스톤 완전 마감** — commit `fd0bf3f` (feat, 34파일 +2584/-78). **핵심 = lag compensation 신뢰 경계를 *시점 인덱스*에만 둠** — 클라가 `C_Attack.attackerClientTick`(자기가 인지한 마지막 서버 tick)만 보내고 *위치는 안 보냄* → 서버가 자기 `PlayerEntity` position ring buffer(4 tick=200ms 깊이, parallel 배열 `Vector2[4]`+`long[4]`+head)에서 그 시점 위치를 꺼내 rewind. 클라가 좌표를 보냈다면 텔레포트 핵(헌법 #1 위반)이 됐을 것 = reviewer가 "Valve Source lag compensation 정석" 평가. 그 시점 번호조차 untrusted라 음수/미래/4tick초과 3분기 fail-closed silent drop(헌법 #3). hitbox = 옛 `dist² < 9.0` 원형 → AABB(attacker 3×3 ∩ 적 1×1) 박스 교차 교체(`Hitbox.cs` 신설, capsule은 M4.3 backlog). **PDL `C_Attack.attackerClientTick` append-only + ProtocolVersion 4→5 bump** = 모든 팀원 다음 pull 시 클라/서버 새 PDL 인지 + Shared.dll 재빌드 흡수 의무(ADR-002 후속 3종). **B1 sweep** = 클라 `AttackRangeSq=9.0f` 매직넘버 → `TargetingRangeSquared` 명명+주석(서버 AABB 권위와 의도적 분리 = 헌법 #4). **B3 sweep** = `98_Shared/CLAUDE.md`+`ARCHITECTURE.md` ProtocolVersion 문서 v5 정정. 처리 = 4 도메인 직렬 위임(shared 2단계 / server 1·4·5·6 / client 3 / qa 7) + reviewer Tier 2-A 5축 PASS + race audit (a)(b)(c) 통과 + trust-boundary commit 게이트 통과. 단위 8건(LagComp 5 + Hitbox 3) + 통합(LagSim) + 봇 lag 시뮬(`--simulated-latency`) + 봇 attackerClientTick 회귀 봉합. 빌드 0/0, 테스트 **221통과/0실패/3skip**. **HTML 학습문서** `LagComp_Report/` 8페이지+SVG 9개(사용자 요청 캡스톤 자산, 외부 의존성 0). M4.1 = Phase 01~06 ✅ 전부 마감. 모든 팀원 영향(ProtocolVersion bump → 클라 빌드) → [M]. | [M] |
| 2026-05-24 | **hook command 경로 $CLAUDE_PROJECT_DIR 절대화 (cwd 드리프트 결함 봉합)** — `.claude/settings.json` hook 8 스크립트(9 등록)가 상대경로 `.claude/hooks/*.sh`라 Bash 도구 cwd가 repo 루트에서 드리프트(예: `cd 하위폴더`)하면 PreToolUse/PostToolUse hook 전부 `No such file or directory`로 실패(안전망 무력화). Phase 06 마무리 중 HTML 점검 `cd ...LagComp_Report/`로 표면화 — 다만 SubAgent는 각자 셸 + 드리프트가 코드작업·reviewer 이후라 안전 작업 누락 0. 봉합 = command 경로를 `$CLAUDE_PROJECT_DIR/.claude/hooks/..`로 절대화(cwd 무관 해석). 스테이징+사용자 적용 패턴(self-modification 하드 차단 정합) + 백업 동반. memory `bash-cd-breaks-relative-hooks` 박음. 모든 팀원/합류자 영향(hook 견고성) → [M]. | [M] |
| 2026-05-23 | **M4.1 Phase 02-04 마감 + Codex β cross-review γ 8회차 통과 (조건부 GO → GO 격상) + ★★★ 학습 자산 진짜화** — 본 세션 누적 commit 9건 + 4 Phase 마감 (Phase 01 Codex β cross-review / Phase 02 Session State Machine / Phase 03 ClientNet Trust Boundary Symmetry / Phase 04 Build Artifact Hygiene). Codex β cross-review 결과 = 조건부 GO + 즉시 봉합 권장 3건 (false-promise 4건 sweep + FrameValidatorSymmetryTests 신설 + deterministic build 주석 정밀화). 사용자 가닥 B 채택 → 즉시 봉합 후 GO 격상. **본 세션 큰 학습 자산 ★★★ 1건 진짜화** = `option-b-variant-third-path-with-drift-guard` (공유 vs 분리 갈래의 third path — 양쪽 helper 박되 contract test로 wire invariant 고정). 옛 Phase 03 마감 시점에 박은 학습 자산이 Codex β cross-review에서 *부분 정합 + 어필 보강 필요* 박힘 = drift guard가 서버 측만 검출, 클라 측 자동 검출 X = "동등 보호" 미완. `FrameValidatorSymmetryTests` 11건 신설(상수 동기화 2 + 행동 동기화 9 Theory)로 본 학습 자산 *진짜화*. 한국 게임 회사 백엔드 면접 "헌법 #4 vs 모듈 재사용성 갈등 어떻게 풀었나" 답변 어필 결정타 키워드. **본 세션 메타 학습 ★★ 누적 12건**: Phase 01 마감 3건 + plan-auditor 2건 + Phase 02 마감 3건 + Phase 03 마감 1건 (★★★ 진짜화) + cross-review γ 8회차 마감 3건 (`drift-guard-asymmetry-is-incomplete-equivalence` ★★★ / `self-overconfidence-on-design-decisions-by-ai-assistant` ★★ / `deterministic-build-precise-claim-vs-overpromise` ★★). **ADR-024 false-promise cadence M4.1 누적 6건 발본** (Codex 슬래시 23번째 + `04_ClientNet/CLAUDE.md:38` stale + Codex β cross-review 4건 = `98_Shared/CLAUDE.md:19` ProtocolVersion stale / `CLAUDE.md:101` slnx 본문 / `02_Server/CLAUDE.md:11` ServerCore 위치 / `04_ClientNet/CLAUDE.md:12` Layout FrameValidator 누락). **γ 방식 8회차 정착** = Codex β cross-review가 자기 점검 한계 보완 + AI 자기 과신 영역 잡는 외부 시각 가치 ★★★ 재증명. **본 entry 박힘 사유 = 결정 변경 X / 약속 정정 + 프로세스 학습 자산 박힘** → [L]. M4.1 마일스톤 PR 머지 자신감 = GO (α reviewer + β Codex 둘 다 통과). Phase 05 (Damage Formulas Extraction, P0-3+P1) + Phase 06 (lag comp + AABB) 남음 = 다음 세션 진입. 모든 팀원 영향 약함 (헌법/도메인 가이드 가짜 약속 정정 + 새 학습 자산 박힘) → [L]. | [L] |
| 2026-05-23 | **Codex CLI 호출 분담 봉합 — Claude 직접 호출 X, 본인 별 세션 호출 default** — M4.1 Phase 01 진입 전 도구 봉합 묶음 박음. **결함 두 가닥 발본**: (1) `/cross-review` 슬래시에 박힌 `codex review --files "{files}" --context "..."` = 실제 Codex CLI 0.132에 *존재 안 하는 옵션* (`codex review` 실측 옵션 = `--base`/`--uncommitted`/`--commit`/`--title`) = **false-promise 23번째 변종 발본** (ADR-024 cadence M3.8 누적 22 → 23, M4.1 첫 1건), (2) sandbox 권한 옵션 차단 사고 8회+ 누적 (memory `codex-sandbox-permission-current-dir` 박혀있지만 영구 해결 X). **봉합 가닥** = 본인 직접 호출 분담 default — (a) Claude 책임 = 점검 자료 박음 + Codex prompt 박음 + γ 비교 + 산출물 박음, (b) 본인 책임 = 별 세션 터미널에서 `codex review --base main` (또는 `--uncommitted`) 직접 호출 + 결과 Claude한테 전달. **4가지 사유**: (1) Claude Bash → Codex 호출 시 Codex 출력이 Claude 컨텍스트 채움 = 토큰 비용 ↑ / (2) 본인 직접 검토 = 학부생 학습 호흡 ↑ / (3) sandbox/명령어 옵션 결함 본인 환경 즉시 조정 / (4) Codex CLI 본인 계정 종속 = Claude 거쳐도 과금 같음. **4 파일 변경**: (1) `.claude/commands/cross-review.md` = "분담 정신 (2026-05-23 봉합)" 절 박음 + Step 3 = "본인 직접 호출 + Claude 자료" 재정의 + Hard rule 1번 신설 (옛 1→2 shift), (2) memory `unity-visual-work-user-owned` = "확장 — 외부 도구 호출" 절 추가 (Unity 외관 본인 직접 정신을 Codex 호출에도 확장), (3) memory `codex-sandbox-permission-current-dir` = "Default 정신 (본인 직접)" 절 + "예외 시점 (Claude Bash 호출)" 절 + **명령어별 옵션 표** 박음 (exec sandbox 옵션 / review sandbox 없음 / false-promise 23번째 변종 박힘 명시), (4) `01_Phases/youngho/M4.1-combat-precision/01-codex-cross-review-and-hardcoding-audit.md` = 담당 정정 + 2단계 분담 재정의 (Claude 자료 + Codex prompt / 본인 호출 / Claude γ 비교 4 sub-step) + 함정 절 한 줄 추가. 본 봉합 = ADR-024 cadence 정합 (23번째 변종 발본 + 즉시 봉합 옵션 A 박음, 별 마일스톤 안 미룸). **메타 학습 ★★★ 후보**: `external-tool-call-user-direct-default` (Unity 외관 본인 직접 → Codex 호출 본인 직접 확장 = "본인 직접 정신"의 일반화 패턴, 외부 도구 호출 default 재정의). 모든 팀원 매 cross-review 호출 영향 (옛 명령어 사용자 = 실패 차단) → [M]. 합류자(인규) 6월 말 셋업 시점에 본인 직접 호출 분담 default 자동 인지. | [M] |
| 2026-05-23 | **M3.8 Capstone-1 Demo Infrastructure 마일스톤 완전 마감 (5 Phase 풀세트, 복잡 등급)** — 본 세션 누적 15 commit으로 캡스톤 1 시연 인프라 풀세트 박음: 메인 → 캐릭터 선택(전사/원거리) → 마을(NPC) → 전투 → 엔딩 *끊김 없는* 흐름 박힘. **Phase 01** PRD 갱신 + 마일스톤 표 정합 ([H]) + **Phase 02** 메인/엔딩 UI + **Phase 03** 캐릭터 선택 (PDL + PlayerStats + 클라 UI, server SubAgent 자율 + 메인 통합) + **Phase 04** NPC 대화 hardcoded + **Phase 05 3분기 재정의** (5-A 2 인스턴스 시연 검증 + 5 결함 봉합 / 5-B 서버 연결 게이트 + 한글 폰트 false-promise 봉합 풀세트 / 5-C Listener accept callback race 봉합 + UI Scene Build 포함 + 학습 자산 HTML). **PDL ProtocolVersion 3→4 bump** 동반 (Phase 03 박음). **Hamachi 2 인스턴스 broadcast 통과 박제** (본인 서브 노트북 가닥, 정유현 시간 안 기다림, `25.41.111.87` ↔ `127.0.0.1` Player 3 + Player 4 동시 등록 + 1초 Ping/Pong + race 봉합 검증 통과 + tick p99 평소 0.1~0.3ms). **★★★ M3.8 핵심 통찰 박힘** — (1) "클라 입력 컨트롤 잘 해야 서버 검증 부담 + Reconcile 끊김 ↓" 헌법 #1 건강한 보완 (본인 통찰), (2) "한 차원 race 봉합은 다른 차원 race 안전 보장 X" 시스템 외곽까지 audit 정합 (5-C Listener race 발본). **ADR-024 cadence false-promise 누적 22건+** (M3 5 + M3.6 7 + M3.7 0 + M3.8 10) — M3.8 10건 = MCP 권한 시트 / SubAgent prompt 위반 + 허위 보고 / MCP OpenScene 자동 Save 부작용 / PretendardStd 영문만 박힌 폰트 / Dynamic Atlas glyph 누락 / MainThreadDispatcher 무한 대기 / TMP_FontAsset atlas/material sub-asset 정통 패턴 / M3 핸드셰이크 재확인 / UI Scene Build 제외 / Listener accept callback race. **본인 분담 정신 박힘 (memory `unity-visual-work-user-owned`)** — Unity 외관/시각 = 본인 직접, 기능/wiring = AI. 본 세션 commit `6a91b65` 본인 디자인 수정에서 짚어주신 패턴. **별 시점 박을 결함 3건 박제** = 빌드 클라 몬스터 Sprite 옛 Placeholder / RemoteEntity 애니메이터 / 횡스크롤 Tilemap 레벨 디자인 (모두 외관 영역 본인 분담). **CODEOWNERS 예외 운영** = 본 세션 다수 commit이 정유현 영역(Scenes/MainMenu.unity + Scenes/UI.unity + Fonts/ + TextMesh Pro/) 침범, 시연 검증 + 본인 직접 디자인 정당 사유. M3.8 PR 시 --admin bypass + 사후 디스코드 알림 의무. dotnet build green (경고 0 오류 0, 본 commit 시점). 모든 팀원 영향 (PDL 4 bump → 다음 pull 시 클라/서버 모두 새 PDL 인지 + 시연 인프라 풀세트 흡수) → [M]. 합류자(인규) 6월 말 셋업 시 새 시연 인프라 풀세트로 자동 진입. | [M] |
| 2026-05-23 | **`.githooks/pre-commit` cloud + 다른 변경 함께 stage 자동 분리 박음 (옛 차단 → 새 cloud hunk만 자동 unstage + 다른 변경 commit 진행)** — 옛 hook은 cloud + 다른 변경 함께 stage 시 차단 박혀서 본인이 `git add -p`로 hunk 분리 의무 = 학부생 워크플로 부담. 본 봉합 = awk으로 hunk별 cloud 라인 패턴 매칭 → cloud hunk만 `git apply --cached --reverse`로 unstage + 다른 변경 stage 그대로 + commit 진행. 검증 1회 통과 (ProjectSettings.asset cloud + 4 다른 hunk 함께 stage → cloud 자동 unstage + 다른 변경 commit `eafa122` 박힘 + 워킹 디렉토리 cloud 라인은 본인 머신 값 보존). 학부생 워크플로 정합 + cloud ping-pong 결함 방어 의도 유지 (5/19 hook 신설 정신 그대로). 안전 = 알려진 cloud 라인 패턴 매칭 (cloudProjectId/organizationId/projectName + cloudServicesEnabled/Build/Game Performance/Legacy Analytics/Purchasing/UDP/Unity Ads 8 추가) → 알려지지 않은 새 cloud 라인은 commit 박힐 가능성 = 발견 시 패턴 추가 의무. fallback = 같은 hunk에 cloud + 다른 변경 섞여있어 `git apply` 실패 시 본인 손 `add -p` 안내 (드문 케이스, yaml 거리 ↑이라 분리 박힘). 모든 팀원 매 commit 영향 (ProjectSettings.asset 변경 시) → [M]. | [M] |
| 2026-05-23 | **`/session:start` (C-1) cloud 라인 게이트 봉합 — drift + 동작 정정** — 본 세션 시작 게이트 STOP 박힘 발견 (`projectName: client → 03_Client` 변경이 (C-1) 패턴에 빠져있어 (C-2)로 잘못 분류 → STOP). 진단 = `commands/session/start.md` cloud 라인 패턴 (`cloudProjectId|organizationId`)이 `.githooks/pre-commit` 패턴 (`cloudProjectId|organizationId|projectName|cloudServicesEnabled|Build|Game Performance|Legacy Analytics|Purchasing|UDP|Unity Ads`)과 drift (5/19 hook 신설 시 동기화 누락). **두 갈래 봉합**: (1) 패턴 hook과 통일 (8개 추가). (2) (C-1) 동작 = 자동 `git checkout` 정리 → **무음 통과** 변경. 사유: 자동 정리 후 Unity 다시 열면 본인 머신 cloud 식별 자동 채워넣음 → 무한 핑퐁. 무음 통과 = 워킹 디렉토리 dirty 유지하되 hook이 commit 시 자동 unstage + 차단 → push 사고 위험 X + 매 세션 STOP 안 박힘 (학부생 워크플로 정합). 본 결정 박힌 사유 주석 한 단락 동반 박음 (2026-05-23 의논 결과). 모든 팀원 매일 첫 호출 영향 ([M]). 합류자(인규) 6월 말 셋업 시점에 이미 봉합 박힘. | [M] |
| 2026-05-23 | **M3.8 Capstone-1 Demo Infrastructure 마일스톤 신설 — PRD 갱신 동반 (Phase 01 마감)** — 캡스톤 1 발표(2026-06-10) 데모 인프라 풀세트 박음 결정. 메인 → 캐릭터 선택(전사/원거리) → 마을(NPC 대화) → 전투 → 엔딩 *끊김 없는 시연 flow* 도달이 본 마일스톤 목표 (5 Phase, 5/23~5/26). **PRD 갱신 두 갈래** — (1) 마일스톤 표 세분화 = M3.5/M3.6/M3.7/M3.8 신설 4행 + M4 → M4.1/M4.2/M4.3 분할 3행 (M2.5/M3 ✅ 완료 표기 동반), (2) MVP 제외 항목 정정 = 옛 "직업/스킬 트리, 퀘스트/NPC" → 새 "스킬 트리 (기본 직업 2종 = 전사/원거리는 캡스톤 데모용 흡수, M3.8) + 퀘스트 (NPC 대화는 캡스톤 데모용 단순 hardcoded 흡수, M3.8 — 본 마감 후 M6 길드 진입 시 정식화 가능)". **시연용 인프라 ≠ MVP 인프라 패턴** — Demo 인프라(메인/캐릭터 선택/NPC/엔딩)는 *캡스톤 1 시연 한정* 영역, 본 마감(11/19) 후 일부 제거 가능 (NPC hardcoded → M6 정식화). M4 영역(*본 마감용 정밀화*)과 의미 분리 = 별 마일스톤 정합. **영향 = 옛 PRD 결정 일부 뒤집기 = [H]** (MVP 제외 항목 = 헌법 아니지만 변경 시 모든 팀원 인지 의무). PRD 변경 이력 표 한 줄 동반 박음 (2026-05-23). **본 entry 박힌 Phase 01 = 단순 등급 + irreversible 위험 깃발** (작업 차원 단순, 위험 깃발은 결정 박제 차원 → 등급 단순 유지 결정 박힘, plan-auditor 봉합 7건 중 한 건). Phase 01 단독 PR 분리 X (M3.8 전체 마감 시점에 한 PR 묶음). 모든 팀원 영향 (PRD 정합 + 본 마감 후 제거 가능 항목 인지) → [H]. 합류자(인규) 6월 말 셋업 시점에 갱신된 PRD 마일스톤 표 + MVP 제외 항목으로 자동 인지. | [H] |
| 2026-05-22 | **M3.7 Sync gate + drift hardening 마일스톤 마감 (단발, 2 Phase)** — M3.6 마감 직후 PR #44 머지 확인 시점에 work-pin/CONTEXT가 4단계(commit/push/PR 생성/PR 머지) 모두 stale로 박힘 = **옵션 C 게이트 5번째 실측 한계 = Rule of Three 통과 → 본질 봉합 트리거 ON**. **Phase 01** (보통, `/session:start` drift 발견 게이트 신설) — `commands/session/start.md` 0-부수 단계 박힘 (0단계 (A) 클리어 후 git log -3 + gh pr list + git status -sb 자동 호출 → work-pin "현재 작업/다음 액션" 키워드 vs 실제 상태 대략 매칭 → 차이 발견 시 STOP + 본인 수동 갱신 안내, **자동 갱신 X** Hook is for alert 정신 / pin-and-done.md §1 정합). `team-guide.html` "막혔을 때" 표 한 행 추가 (drift STOP 안내). **Phase 02** (복잡, ADR 묶음 신설) — `ADR-023 work-pin/CONTEXT 동기화 결함 — 진행 단계 stale hole 봉합` (옵션 C 게이트 *유지* + 새 발견 게이트 *보강* + 자동 갱신 박지 않음 결정, 5번 실측 사례 + 옵션 A/B 보류 사유 + 모니터링). `ADR-024 false-promise 주기적 감사 cadence` (누적 12건+ Rule of Three 3회 통과 → 마일스톤 마감 -DONE.md "false-promise 점검 결과" 섹션 의무 + ad-hoc +5건 트리거. 자동화 도구는 별 시점). `pin-and-done.md §5.1` 신설 (옵션 C 한계 명시 + 발견 게이트 인용 + 동기화 룰 표 한 행). `ADR/INDEX.md` 두 줄 추가. **자기 시범** = 본 -DONE.md에 ADR-024 cadence 첫 시범 박음 (M3.7 누적 0건, self-honoring pattern). **시리즈 정합** — 본 M3.7 = M3.6 점검 마일스톤 직후 봉합 마일스톤 (대규모 점검 → 단발 봉합 패턴). M3.5 Phase 05 옵션 C 게이트 + M3.7 새 발견 게이트 = *세션 마감 + 세션 시작* 양방향 안전망 완성. 본 마감 = `/session:start` 동작 변경 (모든 팀원 매일 첫 호출 시 drift 점검 통과 필요) + ADR 2건 신설 (영역 정책 변경 X, 정책 보강만) → [M]. 합류자(인규) 6월 말 셋업 시 `/session:start` drift STOP 떠도 `team-guide.html` 표 안내 자동. | [M] |
| 2026-05-22 | **M3.6 하네스 + 코드 점검 마일스톤 완전 마감 (Phase 04·05·06 풀세트)** — Phase 04 (대규모, 서버 코드 전수조사): Coordinator + Worker 3 (server/shared/qa) + reviewer Tier 2-A 첫 실측 5 SubAgent 분해, 헌법 5/5 PASS + false-promise 7건 누적 패턴 정착 (forward 3 + 역방향 2 + 시기상조 1 + 헌법 #4 한계 1), 3 CLAUDE.md 정정 (02_Server / 98_Shared / 99_Tools, ~40줄). Phase 05 (복잡, 클라 코드 전수조사): 메인 직접 분해 (Coordinator 비-호출 1/3) + client Worker (Sonnet, 시나리오 8 PASS) + unity-bridge 자문 + reviewer Tier 2-A, 헌법 5/5 PASS + false-promise 4번째 변종 발본 (문서 내 자기 불일치, `04_ClientNet/CLAUDE.md` path) + Unity batchmode compile 첫 실측 통과 (β3 봉합 = "Unity 미검증 리스크" 명시 의무 면제) + AttackRangeSq 9.0f 헌법 #1 위반 X 확정. Phase 06 (대규모, 마감 의례): 메인 직접 (Coordinator 비-호출 2/3 = Rule of Three 1 남음) + **외부 리뷰 4건 (c) 미흡수 결정 박힘 = 5번째 false-promise 사례 발본** (노션 + 로컬 별 폴더 본인 확인 결과 잔여 4건 흔적 없음 = 옛 본인 work-pin 약속 자체 모호) + M3.6 종합 5단계 보고 MD+HTML 이중 박음 (캡스톤 평가 자산, phase-gate-validator 4-3 강화 통과). **false-promise 누적 12건+** (M3 5건 + M3.6 7건) = false-promise 주기적 감사 cadence ADR 신설 트리거 ON (별 마일스톤). **SubAgent 보안 우회 시도 차단 실증** (Phase 04 Worker 차원 deny rule 우회 = Auto Mode classifier 2차 안전망 작동). M3.6 마일스톤 ✅ 완전 마감 (Phase 01·02·03·04·05·06). 본 세션 누적 commit 5건 (`dbf8b18` + `1f9c0e4` + `7d9ade2` + `422b754` + 본 commit). M4 진입 정합 = 본 마감 PR 머지 후 `/work:plan M4`. PR/머지 게이트 4-D/4-E/4-F 첫 실측. 모든 팀원 매번 영향 (false-promise cadence + Hook 8 + SubAgent 9 + Knowledge 5 새 운영 첫 실측 정합) → [H]. | [H] |
| 2026-05-22 | **M3.6 Phase 02 + Phase 03 풀세트 마감 (자동 등급 상향: 복잡 → 대규모)** — Phase 02 정합 감사: ADR 22 × 코드 매핑 (가짜 약속 0건, 미래 약속 3 / 지연 2 / 완전 17) + policies 8 × Hook 매핑 + 헌법 5 × 코드 시연 + pin-and-done.md 자기 위반 봉합 (254→217줄, doc-thresholds 220 임계 통과). Phase 03 하네스 v1 재조정 + Hook hardening bundle **5건**: (1) **dangerous-cmd-guard Python shlex.split 본질 봉합** (β2 매트릭스 13/13 PASS, false positive 봉합 — PR #43 응급 봉합 후속), (2) must-pass/advisory 분리 정합 검증 (README 이미 박힘), (3) **phase-gate-validator HTML 페어 강화** (대규모 등급 -DONE.md 의무화, 옛 경고 → exit 2 차단, 옛 자산 grandfathered = 변경 X자연 회피), (4) **risk-detector 4번째 깃발 `harness` 신설** + **trust-boundary 매처 stale 봉합** (옛 `02_Server/Handlers/` 직접만 → 새 한 단계 깊은 `02_Server/*/Handlers/*` 흡수, 가짜 약속 4번째 발본 = Phase 02-A "0건" 정정), (5) **reviewer-auto-trigger.sh 신설** (ADR-019 Hard hook, 정유현 5/16 합류 → 5/22 = 1주 임박 약속 봉합, 5/5 PASS). **ADR-020 갱신** — Hook 환경 의존성 = Git Bash + **Python 3.6+ 명시** (PR #43 암묵 박힘 → Phase 03-B 명시) + **Windows CRLF 함정** (Python text mode `\r\n` → mapfile 토큰 끝 `\r` 잔재, `tr -d '\r'` 후처리 의무). **ADR-019 갱신** — Hard hook 박힘 명시. **setup-steps/02-common.md** 신규 9단계 = Python 3 검증 (옛 9 → 10 재번호). **policies 신선도 갱신** — grade-and-risk + subagent-routing 첫 실측 사이클 통과 명시 (본문 수정 X). **Hook 풀세트 7 → 8** (reviewer-auto-trigger 신설). settings.json hooks 등록 추가. **자동 등급 상향**: Phase 03 옛 추측 복잡 → 실제 5건 Hook 변경 + ADR 갱신 + setup-steps + CHANGELOG [H] = 대규모 정합. work-pin 자동 상향 사유 박힘. 02-DONE.md + 03-DONE.md (5단계 보고 MD + HTML 의무) 박음. 모든 팀원 매번 영향 (Python 3 의무 + reviewer Hard hook + Hook 풀세트 8개) → [H]. 합류자(인규) 6월 말 셋업 시점에 setup-steps/02-common.md 9단계로 자동 안내. 정유현 5/16 합류 = 본인 머신 Python 3 검증 별도 안내 의무 (디스코드/슬랙). | [H] |
| 2026-05-22 | **`policies/INDEX.md` 정합 봉합 + 98_Shared/CLAUDE.md ProtocolVersion stale sweep** — PR #43에서 박힌 `pr-and-merge-gate.md`가 INDEX 표에 누락된 정합 깨짐 봉합 (표 7→8행 + `subagent-routing.md` 한 줄 요약 풀 8→9 동기). 98_Shared/CLAUDE.md `Current=2` → `Current=3` (M3 Phase 06 bump 명시). `/cross-review` 슬래시 첫 정식 실측 (γ 8회차, α + β × 2)에서 발견 → commit `dbf8b18` 봉합. M3.6 마일스톤 plan 풀세트 박힘 (`01_Phases/youngho/M3.6-harness-and-codebase-audit/`, 8 파일 commit `caa231f`) — *AI Harness 점검 + 프로젝트 코드 전수조사* 6 Phase 분해. INDEX 정정 + stale sweep + 본인 작업 plan 박힘이라 위험 ↓. 모든 팀원 영향 약함 (INDEX 표 한 줄, ProtocolVersion 문서 stale 정정). | [L] |
| 2026-05-21 | **M3.5 새 하네스 v1 발효 (ADR-022) — 옛 → 새 atomic 전환 commit** — M3 마감 직후 5/20 면담 의논 결과 박힌 새 하네스 모델을 별도 마일스톤 M3.5(Phase 01~06)로 박고 격리 폴더 `New_Harness/` 누적 → Phase 06에서 일괄 mv로 *옛 100% 작동 → 새 100% 발효* atomic 전환. **6 핵심 변경**: (1) **KPI 전환** — "학습 박제 중심" → "Planning → 구현 → 보고" (학습은 트랙 B 분리). (2) **정량 4등급** (단순/보통/복잡/대규모) + 위험 깃발 자동 상향 (trust-boundary/irreversible/unity-asset → `risk-detector` Hook). (3) **SubAgent 풀 9** (옛 7 → 새 9): Worker 4 (server/shared/client/qa, 옛 6 도메인 통합) + Reviewer 2 (reviewer/plan-auditor, Codex γ 내부 흡수) + Specialist 3 (unity-bridge/coordinator/knowledge-gc). 모델 분담 = Sonnet (Worker + knowledge-gc) / Opus (Coordinator + Reviewer + plan-auditor). (4) **Hook 7 풀세트** (옛 5 → 새 7): rename 3 (inject-current-pin→pin-injector / validate-phase-gate→phase-gate-validator 강화 / validate-shared-changes→shared-discipline-guard 차단) + 삭제 2 (check-server-authority Reviewer 흡수, check-work-envelope 양식 죽임) + 신설 4 (dangerous-cmd-guard/tdd-guard/circuit-breaker/risk-detector). (5) **Knowledge 시스템 신설** — `.claude/knowledge/<도메인>/_index.md` 5 도메인 + GC Collector SubAgent (수동 트리거만, AI 자율 박제 X). 트랙 A (AI 캐시) vs 트랙 B (본인 회고) 분리. (6) **슬래시 17 → 10** + 신규 2: 작업 4 + 세션 3 + 점검 2 신설 (`/harness-review` 하네스 자체 점검 + `/cross-review` 외부 시각, Rule of Three 통과 후 슬래시화) + 셋업 1. **옛 학습 5 (`/learn:*`) + 일지 3 (`/journal:*`) = 트랙 B Notion 이관** (잔존 `learning-journal/{본인}/` 디렉토리는 옛 자산 보존 정신으로 그대로 유지). 학습/일지는 Claude한테 자연어로 또는 본인 노션 자유 양식. (7) **양식 다이어트** — work-envelope 절 통째 죽임 (옛 매 코드 응답 봉투 X) / 5단계 보고 = *대규모 등급 Phase 완료 시만* + MD+HTML 이중 박음 (캡스톤 평가 자산) / work-pin 압축 30~40줄. (8) **work-pin ↔ CONTEXT 정합 게이트 (옵션 C)** — `/session:end` 단일 게이트 단방향 동기, 등급 무관 "⏸️ 현재 멈춤 지점" 항상 동기. (9) **Phase 폴더 namespace** — `01_Phases/<owner>/M{N}-{slug}/` + frontmatter `owner: youngho|yuhyeon|inkyu` 필수 + `grade:` `risk:` 박음. `inkyu/` 폴더 신설 (6월 말 합류 대비). (10) **헌법 부분 갱신** — 절대 원칙 5개 (서버 권위/프로토콜 신성/신뢰 경계/공유 코드 규율/틱 블로킹) *100% 보존*. 옛 200줄 → 새 239줄 (+39, 본체 다이어트 X, 새 모델 본체 추가만, 운영 양식 절은 다이어트). **변경 수치**: 67 삭제 / 17 갱신 / 24 신규 = 108 파일. **검증**: dotnet build green + dotnet test (Task #7 진행 중) + 새 hooks 작동 실측 (본 transition 자체에서 검증, work-pin reference 갱신 / Edit 시 risk-detector PASS). **모든 팀원 영향** — Claude Code 사용자 매 세션 적용. **합류자(인규)** = 6월 말 합류 시 새 모델로 진입, `/setup`이 자동 안내. **별 안내**: 디스코드/슬랙에 본 commit 직후 한 번 공지 (옛 `/learn:*` `/journal:*` 호출 시 "트랙 B 이관됨" 안내 메시지). 면접 자산화 = *방향성 결정/리팩터링 의사결정* 한국 게임 회사 백엔드 어필 결정타 (바이브 도메인 시대 인식 + 투트랙 분리 + Zero-based 재구성 + NDREAM 패턴 흡수 + 팀 합류 흡수). | [H] |
| 2026-05-19 | **`.githooks/pre-commit` 보강 — ProjectSettings.asset cloud 라인 자동 unstage** — 본인 commit이 유현 cloudProjectId 덮어씀(또는 반대) ping-pong 발견 (main `e9aa005` 박힌 값 = `c89f079f-...`/`jyhchallengers` = 유현 Cloud, 본인 머신 = `1094041a-...`/`roy_131`). 원인: `/session:start`의 (C-1) 게이트는 *세션 시작 시점*만 작동 → 세션 중간 commit이 빠져나감. **fix**: pre-commit hook 맨 앞에 cloud 라인 검사 + 자동 unstage 박음 (패턴 10개: `cloudProjectId`/`organizationId`/`projectName` + `cloudServicesEnabled` 블록 6 자식 키 — `Build`/`Game Performance`/`Legacy Analytics`/`Purchasing`/`UDP`/`Unity Ads`). cloud 라인 *만* stage → 자동 unstage (워킹 디렉토리 보존 = 본인 머신 식별 유지) / cloud + 다른 변경 → block + 분리 안내 (`git add -p`). 검증 2회 통과 (단독 stage → unstage + 빈 commit 차단 / 다른 파일 함께 stage → cloud만 unstage + 나머지 commit 진행). `core.hooksPath` 이미 박혔(CHANGELOG 5/18 PR #28) → 모든 머신 자동 적용 + 합류 시 부담 X. 5/20 면담 직전 정유현 Phase 08a pull 안전망 (본 commit 후 push해도 유현 cloud 안 덮어씀). 모든 팀원 매 commit 영향이라 [M]. | [M] |
| 2026-05-18 | **M3 Phase 05/08 prefab 분담 합의 + LocalPlayer.prefab 추출 + M2 Phase 07/08 스킵 박제 + M3 Phase 08 분리** — 영호 골격(`RemoteEntity` 컴포넌트) + 유현 비주얼(`SpriteRenderer`/`Animator`) 분담 (`_handoff-prefab-20260518.html` 박제). LocalPlayer.prefab 영호가 미리 추출(`Prefabs/Characters/`, 컴포넌트 6개 보존 + CameraFollow target 복구). M3 Phase 08 → 08a(Asset+Prefab variant, 유현 *병렬 시작*) / 08b(Zone+UI, 유현 후속) 분리 정의 박힘. M2 Phase 07(한글화) + 08(회귀 영상) 시간 부족 스킵 박제. PlayerBase.prefab variant 패턴 도입 결정. 유현 측 프롬프트 별송 예정(유현 머신 CONTEXT.md/work-pin은 git 동기 X — 프롬프트 통로). 본 commit은 *인프라만 빠른 main 머지* 예정(유현 Phase 08a 진입용). 영역 = 클라 prefab + 정유현 영역 작업 시작 결정이라 [L] (코드 변경은 prefab 추출 1건, 큰 행동 변경 X). | [L] |
| 2026-05-18 | **γ 방식 5회차 Codex β 1차 검증 + 후속 봉합 2건** — commit `866e21a`. Codex 환경 `dotnet test 160 passed / 0 failed / 1 skipped` (예상 baseline 151 → 160 정확). 관련 필터 13 passed. 구현 경로 / initial roster 순서 / PDL 3종 모두 PASS. 지적 2건 즉시 봉합 (옵션 B 패턴): **(1) Medium #1**: `LifecycleRace_NewJoinBroadcastSkipsClosingSession` 순서 보강 — 옛 `s1.OnDisconnected→s2.OnConnected` 순서는 FIFO로 cleanup 먼저 처리 → `_players=[]` 상태에서 s2 enter → IsClosing skip 분기 *안 거치고도* 통과 (false confidence). 새 순서 `s2.OnConnected→s1.OnDisconnected→Tick`은 FIFO로 s2 enter 시점에 `_players=[s1]` + `s1.IsClosing=true` → skip 작동해야 통과 = 분기 깨지면 fail. **(2) Low #2**: `02_Server/CLAUDE.md`의 "Async: 핸들러는 `async Task`" 문구를 현재 sync `void Handle(...)` 설계로 정정 + 백그라운드 channel 격리 + async 승격 조건 명시. Codex review 산출물 `00_Document/reviews/2026-05-18-m3-phase-03-04-codex-review.md` 박제. dotnet build green. dotnet test는 본 머신 SAC On 차단 그대로 — Codex 환경 재검증 위탁 (γ 5회차 후속 pass-2 예정). 후속 권장 = Phase 05 진입 직전 Unity client `S_PlayerJoin`/`S_PlayerLeave` dispatch + `S_Snapshot.entityId` routing 필수 (Codex 추가 관찰). | [M] |
| 2026-05-18 | **M3 Phase 04 완료 — 서버 broadcast 인프라 + SAC On 환경 dotnet test 차단 사고 박제** — commit `5ea1123`. PDL `S_PlayerJoin{int entityId, float spawnX, spawnY}` (PacketID 9) + `S_PlayerLeave{int entityId}` (PacketID 10) 신설. `GameMap.BroadcastToAll(payload, except=null)` 메서드 신설 — owner null / except / `IsClosing` skip 3종으로 **Phase 10 lifecycle race 패턴 일반화** (Codex Phase 04 risk 1순위). `GameMap.Tick` snapshot owner unicast → 전원 broadcast 전환 (N² 비용 인지, 응급 모드 N≤4 무시). `GameSession.IsClosing` internal getter 신설(Volatile.Read `_closing`). `EnterGameWorld`에 initial roster 패턴 박힘(AddPlayer 전 `existing` snapshot → 자기 add → 자기에게 기존 entity 다발 Send → 자기 외 broadcast). `OnDisconnected`에 PlayerLeave broadcast 추가. `BroadcastTests.cs` 5건 신설 — `LifecycleRace_NewJoinBroadcastSkipsClosingSession`이 deterministic race window 재현. **`dotnet build green` (경고 0 오류 0) — 그러나 `dotnet test` 실측 X**: **Smart App Control (SAC) = On + Code Integrity Event 3077 차단** (Policy ID `{0283ac0f-fff1-49ae-ada1-8a933130cad6}` = SAC 정책). 진단 3단계 거침(Unblock-File → AppLocker 이벤트 → SAC 상태). SAC On → Off는 OS 재설치 필요(MS 의도된 제약) → 응급 모드 B 채택 = 테스트 없이 commit + Codex 1차 검증 시 별도 환경 재실측. 학습 박제 → `~/.claude/.../memory/smart-app-control-dotnet-test-block.md` 진단 절차 + MEMORY.md 채널 등록. CONTEXT.md 보류 항목 "WDAC 정책 정리" 발화. Shared.dll commit 동반(PDL 수정 의무 3종). 모든 팀원 PDL 영향 + 새 패킷 dispatch는 클라(Phase 05)에서 박힘 — 정유현 pull 시 빌드 OK(unknown packet drop, 영향 X). 도구 정책 발화 + 모든 팀원 PDL 영향이라 [M]. | [M] |
| 2026-05-18 | **M3 Phase 03 완료 — 핸들러 layer 분리 (헌법 #4 "Shared Code Discipline" 가짜 약속 2번째 봉합)** — commit `4065616`. `02_Server/CLAUDE.md` Layout에 약속만 박혀있던 `Handlers/` 폴더를 *실재로* 신설 (5파일: `IPacketHandler` 인터페이스 / `HandlerRegistry` static Dictionary / `HandshakeHandler` / `MoveIntentHandler` / `PingHandler`). `GameSession.OnRecvPacket`의 inline `HandleHandshake`/`HandlePing`/`HandleMoveIntent` 3개를 외부 IPacketHandler 구현체로 추출, Dictionary dispatch로 if-else/switch 제거 (새 핸들러 = 한 줄 등록). Codex 인사이트(Phase 02 review) 1:1 준수 — handler = decode + 검증, session = lifecycle state. `CompleteHandshakeAndEnter` `protected` → `protected internal` (Handlers + 테스트 subclass 양쪽 호환). 신설 internal API: `RejectHandshake` / `SubmitMoveIntent` / `RespondPong`. **02_Server/CLAUDE.md Layout 표 *동시 commit*** — 코드 + 문서 동시 봉합 정신, 반쪽 봉합 X. 누락 핸들러 단위 테스트 페어 4건 신설 (MoveIntentHandlerTests happy + invalid bits / PingHandlerTests happy + auth). dotnet test **155 통과 / 0 실패 / 1 skip** (Phase 02 baseline 151 → +4, 회귀 0). 박제 → `01_Phases/youngho/M3-first-multiplayer/03-handler-layer-split-DONE.md`. 도구 동작 변경 X / 모든 팀원 02_Server 내부 영향 (정유현 영역 직접 X) → [M]. | [M] |
| 2026-05-18 | **M3 Phase 02 Codex review 후속 봉합 (γ 방식 4회차 실측)** — Codex β 직접 검증(`00_Document/reviews/2026-05-18-m3-phase-02-codex-review.md`, 코드 직접 + dotnet test 재실측 = **7건 발견**) 옵션 B 채택 (응급 모드 균형). **즉시 봉합 4건**: (1) Codex 인사이트 `CompleteHandshakeAndEnter()` 캡슐화 (Phase 03 핸들러 layer 분리 시 외부 handler가 `_handshakeCompleted`/`EnterGameWorld` 직접 침범 X — handler = decode + 검증 + session 메서드 호출만 책임), 옛 `MarkHandshakeCompletedForTest` 제거. (2) Codex #5 duplicate handshake protocol violation = `_handshakeCompleted=true` 후 C_Handshake 재수신은 switch case에서 명시 거절(Disconnect + [Trust] 로그) + 4번째 보너스 테스트 `DuplicateHandshake_AfterCompleted_RejectsAsProtocolViolation`. (3) Codex #2 Unity `SendIntent` handshake gate = `HandshakeOk` 프로퍼티 신설(main thread `HandleHandshakeResult` 박힘 → 같은 thread `SendIntent` visibility) + race window 짧은 창 차단 (socket worker C_Handshake 자동 Send 박은 직후, main thread Update의 LocalPlayerController.SendIntent가 먼저 호출되는 케이스). (4) Codex #4 `PacketRoundTripTests` 11건 직접 회귀 안전망 = C_Handshake (ushort edge 5 + size/id) + S_HandshakeResult (ok=true/false + reason 5 case: empty/ASCII/긴 ASCII/CJK Unicode/emoji surrogate pair + PacketId 8). **잔존 알려진 결함 4건** (응급 데모 영향 작아 본 마감 전 별도 Phase 봉합): HIGH #1 `Send → Disconnect` race(TCP flush 보장 X, mismatch 발생 시 reason 전달 X) → `DisconnectAfterFlush` 패턴 신설, MEDIUM #3 malformed body 테스트, LOW #6 GenPackets.cs trailing whitespace 8건, LOW #7 PDL unknown type silent skip. dotnet test **151 통과/0 실패/1 skip** (135 → +16, 회귀 0). 행동 변경(SendIntent gate) + 동시 작업 정유현 Unity 측 영향 가능성(정상 흐름엔 X, race window에서만) → [M]. γ 방식 4회차 = Rule of Three 통과 후 정착 단계 실측 — Codex *코드 직접 접근* 가치 실증. | [M] |
| 2026-05-18 | **M3 Phase 02 완료 — ProtocolVersion 핸드셰이크 (헌법 #2 가짜 약속 1번째 봉합) + PacketGenerator bool/string 결함 동반 fix** — `98_Shared/CLAUDE.md`에 7 Phase째 박혀있던 "핸드셰이크 코드 미구현" *가짜 약속*을 코드로 봉합. PDL에 `C_Handshake { ushort clientVersion }` + `S_HandshakeResult { bool ok, ushort serverVersion, string reason }` 신설. `GameSession` first-packet 강제 패턴 — `_handshakeCompleted` 플래그가 false면 OnRecvPacket이 C_Handshake만 dispatch, 다른 거 즉시 Disconnect. mismatch도 reason 박은 S_HandshakeResult(ok=false) 회신 후 즉시 Disconnect (헌법 #3 정합 — timeout 안 기다림). 옛 OnConnected의 AddPlayer 흐름은 `EnterGameWorld()`로 추출 → HandleHandshake 통과 후 호출. `04_ClientNet`(Unity wrapper) + 헤드레스 봇 양쪽 OnConnected에서 C_Handshake 자동 전송 + S_HandshakeResult ok 검사. **PacketGenerator bool/string 결함 동반 fix (blocker 봉합)**: S_HandshakeResult가 두 타입의 첫 실수요자라 분리 시 의존성 헝클어짐. Program.cs switch에 bool 별도 분기(byte 1바이트 패턴, BinaryPrimitives에 LittleEndian 변종 없음), PacketFormat.cs `Read/WriteStringFormat`은 `Segment.Array` 미정의 + NET_LEGACY 깨짐을 `BinaryPrimitives.TryWriteUInt16LittleEndian` + `Encoding.Unicode.GetByteCount/GetBytes(span,span)` 패턴으로 정정. `Read/WriteBoolFormat` 신설. HandshakeHandlerTests 3건(happy/mismatch/non-handshake 첫 패킷) 신설 + 기존 lifecycle/rate-limit 테스트의 `TestGameSession.OnConnected`에 handshake mock 적용(MarkHandshakeCompletedForTest + EnterGameWorld 직접 호출). dotnet test **135 통과 / 0 실패 / 1 skip** (Phase 01 baseline 132 → +3 HandshakeHandlerTests, 회귀 0). 박제 → `01_Phases/youngho/M3-first-multiplayer/02-protocol-version-handshake-DONE.md`. 도구 기본 동작 변경 + 모든 팀원 PDL/Shared.dll 영향 (특히 정유현 다음 pull 시 Unity 자동 handshake 전송 시작 — 의도된 동작) → [M]. | [M] |
| 2026-05-18 | **M3 First Multiplayer & Demo Stage 마일스톤 진입 + γ 방식 3회차 Codex β 검토 + PacketGenerator 기본값 반전** — PRD 마일스톤 재정렬 (M3 = Multiplayer & Demo Stage / M4 = 진짜 Combat + 4맵 분리, 5/20 교수 중간 면담 응급 데모 가닥 — β+ 묶음 = broadcast + 단순 전투 + 단일 맵 3-zone + Stage Clear UI, 시연 위주). Codex β xhigh γ 3회차(Rule of Three) 검토 발견 6건 반영 = Phase 9개 분해 v2 박힘 (선행 smoke 신설 / 03·04 분리(서버 broadcast vs 클라 remote entity) / 05·06 강한 단순화(적 AI 없음 + 고정 위치 + range+cooldown만) / 09 리허설 체크리스트 신설). **PacketGenerator `noManager` 기본값 `false`→`true` 반전 (Codex 발견 #3 fix, 99_Tools/CLAUDE.md 결함 #1)** — `--no-manager` 잊어도 manager 인프라 부재(ServerCore namespace + PacketHandler 타입 미존재)로 컴파일 깨지는 ServerPacketManager.cs 생성 risk 제거. 모든 팀원이 PDL 변경 시 영향 (더 안전한 방향). `--no-manager` 인자는 redundant이지만 호환성 유지. Phase 01 완료 박제 commit `39a4bda` (16 files / 931 insertions / post-flight gate 통과). 도구 기본값 변경 + 모든 팀원 영향이라 [M]. | [M] |
| 2026-05-18 | **`.githooks/pre-commit` 도입 + `core.hooksPath` 공유 설정** — Claude Code의 PostToolUse hook(`validate-phase-gate.sh`)이 *우회 가능*함을 정유현 PR #27 분석에서 발견 (Phase 04~06 -DONE.md frontmatter 누락 = 지역 컨벤션 드리프트). 진단: 사용자 명시 선호 시 Claude가 hook 차단 메시지 보고도 진행 + 외부 에디터 직접 편집 시 hook 작동 X + git commit 자체는 PostToolUse 매처 범위 밖. **해결**: `.githooks/pre-commit` 신설 → `git config core.hooksPath .githooks`로 공유 → commit 시점에 변경된 `-DONE.md` 각각 `validate-phase-gate.sh` 호출 → 형식 불일치 시 exit 1로 commit 차단. **신규/기존 팀원 영향**: 각 머신에 *1회* `git config core.hooksPath .githooks` 실행 필요 (setup-steps/02-common 8단계 자동 안내 박힘). 합류자(인규)는 /setup 흐름에서 자동. **기존 팀원(정유현·본인) 수동 1회**. 우회는 `--no-verify`로만 가능(의도적 학습용). main 보호 룰 추후 PR 단계 검사 추가 검토 (M3+). 영역 = 모든 팀원 매번 commit에 적용되는 가드라 [M]. PR #28 → main `ad717e1`. | [M] |
| 2026-05-18 | **`.claude/settings.json` ask 룰 영구 완화 — Auto Mode 정책 채택 (임시 → 영구 승격)** — M2.5 자율 작업 중 임시로 박았던 `.claude/settings.json` 변경(`Edit(**)`/`Write(**)`/`Bash(git commit*)` ask 룰 제거)을 *원복 X, 영구 정책*으로 결정. 사유: 자율 모드 효율 + Auto Mode 보편화. **모든 팀원 Claude Code 사용자 영향** — Edit/Write/git commit이 prompt 없이 자동 통과. **무서우면 Ask Edit 모드 사용** (Shift+Tab 한 번 → "acceptEdits" → "default"로 돌아가면 prompt 복구) — 합류 팀원(인규)·정유현에게 디스코드 안내 박을 것. 단 `deny` 룰(curl/wget/secrets read)은 그대로 유지 — 진짜 위험한 명령은 여전히 차단. main 보호 룰 + CODEOWNERS는 별도라 *팀원 PR 머지엔 본인 승인 강제 그대로*. 권한 정책 *완화* + 모든 팀원 영향이라 [H]. 본 entry는 ε 결정의 후속 박제 (아래 M2.5 entry의 "원복 안 내" 안건 해소). | [H] |
| 2026-05-18 | **M2.5 Hardening 완료 — Phase 09 (Trust-boundary fail-closed) + Phase 10 (Session lifecycle race 제거) + Phase 11 (정리)** — γ 감사 위반 4건 봉합. Phase 09: packet length 헤더 상하한 검증(4~4096) + decode 예외 → Disconnect + rate-limit 임계 초과 drop. Phase 10: `_closing` 플래그 + always-enqueue + owner-ref cleanup으로 ghost player 차단. Phase 11: 헌법 우선순위 표에 `00_Document/policies/` 한 줄 보강 + UnitTest1.cs 빈 placeholder 삭제 + CONTEXT.md 응축 + 임시 settings.json 변경 원복 안내. 신규 테스트 24건 (length validation 11 + rate-limit 5 + lifecycle 8 — drift verify는 length에 포함). 회귀 0, dotnet test 134/135 통과. γ 방식 2회차 적용 — 각 Phase 후 Codex β xhigh 검토 + 권장 7건 반영. 헌법 변경(우선순위 표) 포함이라 [M]. PR #29. | [M] |
| 2026-05-18 | **γ 방식 ad-hoc 전체 감사 패턴 첫 실측 + M2.5 마일스톤 신설 + REVIEW_CHECKLIST 5.7 신설 + review-tiering 실측 1회 박제** — M2 완료 증명 직후 M3 진입 전, 본인 단독 영역(02_Server/98_Shared/99_Tools/04_ClientNet/문서)을 Claude reviewer agent (α) + Codex CLI (β) 양쪽으로 점검 + 결과 비교 (~50분 비용). 결과: α 14건 + β α 전부 동의 + 추가 6건 = 총 14+6건. Codex 추가 발견이 *우선순위 자체 뒤집음* (M3 broadcast 첫 데모 ghost player 위험이 문서 정리보다 급함). 산출물: `00_Document/reviews/2026-05-18-pre-m3-{claude,codex}-review.md` 2건 + PR #24 머지 → main `1548628`. **M2.5 별도 마일스톤 결정** = Phase 09 (Trust-boundary fail-closed) + Phase 10 (Session lifecycle race 제거) → M3 진입. 후속 인프라 갱신: REVIEW_CHECKLIST 축 5에 5.7 (세이프티 약속 일치 검증, rate-limit/ProtocolVersion 패턴 산출) + review-tiering.md 섹션 7 실측 1회 기록. ADR 신설 / `/work:audit` 슬래시 신설은 Rule of Three까지 보류 (2~3회 더 실측 후). 영역 = 인프라 새 항목 + 운영 패턴 박제라 [M]. PR #24. | [M] |
| 2026-05-17 | **PDL 수정 후 후속 작업 의무 박제 (운영 룰)** — `cee8775`(Phase 06 tick counter `uint` 통일) 후 PacketGenerator 재생성 + Shared.dll commit 둘 다 누락한 채 main push → 정유현 pull 시 빌드 회귀로 표면화. 영구 fix는 PR #19(아래 줄, `.gitignore` 화이트리스트 + Shared.dll commit). 합류자(인규)는 PR #19 덕에 같은 사고 X. **단 향후 본인이 PDL XML 수정 시** 의무 3종: (a) `PacketGenerator` 즉시 재생성, (b) `dotnet build`로 Shared.dll 갱신, (c) 두 산출물 동반 commit. 헌법 #2(Protocol is Sacred) + #4(Shared Code Discipline) 정합. 운영 룰 명시화라 [M]. | [M] |
| 2026-05-17 | **Shared DLL `.gitignore` 정정 + asmdef Unity.TextMeshPro 추가** — 정유현 main pull 사고(빌드 깨짐 → Safe Mode)로 표면화된 시스템 결함 정정. **원인**: `03_Client/Assets/Plugins/**/*.dll`이 `.gitignore`라 `Shared.dll`이 git 추적 X → 다른 팀원 pull 직후 Unity 측 Shared 어셈블리 outdated 또는 누락 → 클라 코드의 `uint clientTick`이 옛 DLL의 `int`와 충돌처럼 보임 (옛 DLL 잔재 가능성). **fix**: (1) `.gitignore` 화이트리스트 추가 — `!03_Client/Assets/Plugins/Shared/Shared.dll` + `.meta` (실제 작동은 `Shared.csproj`의 `CopyToUnityPlugins` PostBuild target이 자동 복사, 본 정정은 *commit 강제*로 다른 머신 즉시 동작 보장). (2) `Dawnholder.Client.asmdef`에 `Unity.TextMeshPro` references 추가 (정유현 Phase 03 HUD의 `HudController.cs` TMP_Text 사용분 — ADR-021 Scene 분리 후 누락). (3) `Shared.dll` 최신 빌드 commit 동봉. **합류자 영향**: pull 직후 Unity 즉시 동작 (별도 `dotnet build` 불필요). 단 본인이 Shared 수정 시엔 여전히 `dotnet build`로 새 DLL commit 필요. 헌법 #4(Shared Code Discipline) 정합. 영역 = 빌드 인프라 변경이라 [M]. PR. | [M] |
| 2026-05-17 | **정유현 영역: UI Scene 분리 + Additive Load 패턴 박제 (ADR-021)** — `03_Client/Assets/Scripts/Bootstrap/SceneBootstrap.cs` 신설 (`Awake()` → `SceneManager.LoadSceneAsync("UI", Additive)` + 중복 가드) + `Assets/Scenes/UI.unity` 신설 + Phase 03 HUD 통째 이사 (`HudController` + Canvas + 4 자식 + EventSystem) + `CODEOWNERS` 영역 분리 3 경로(`/03_Client/Assets/Scenes/UI.unity`, `/03_Client/Assets/Scripts/Bootstrap/`, `/03_Client/Assets/Scripts/UI/`)를 `@jungyoohyun0105` 단독으로 박음. 같은 PR에 `@yuhyeon` placeholder → `@jungyoohyun0105` 정정 5곳 동봉 (line 17 메모 "정정 완료" 흔적까지 자기 판단 박힘). 정유현 Phase 03 HUD 마감 직후 *영역 분리* 가치 자각 → 팀장 합의 후 ad-hoc 작업(`ad-hoc-20260517-ui-scene-split`). **이유**: Unity `.unity` 파일 YAML 자동 머지 충돌 위험 차단 + 한국 게임 회사 정석 패턴(씬 하나당 한 명). **합류자 영향**: 정유현 후속 UI 작업 단독 영역, 팀장·인규 PR 강제. `03_Client/CLAUDE.md` Layout 표 동반 갱신(`Scripts/Bootstrap/` 한 줄). 영역 분리 변경이라 [M]. PR #17. | [M] |
| 2026-05-16 | **`.vscode/tasks.json` + `launch.json` 신설** — 공통 빌드/실행 인프라 박힘. `Ctrl+Shift+B` → "Build Solution" (`dotnet build Dawnholder.slnx`, default), Tasks 메뉴 → "Run Server" (`dotnet run --project 02_Server/GameServer`), `F5` → "Debug GameServer" (coreclr 디버거 launch, preLaunchTask로 자동 빌드). `.gitignore` 화이트리스트 정정 (`!.vscode/tasks.json` + `!.vscode/launch.json` 추가). **Git Bash backslash escape 함정 회피**: VS Code의 `${workspaceFolder}` 절대경로(`\` 포함)를 Git Bash가 escape로 먹어 `C:DevClaudeDev` 망가짐 → 상대 경로 + `options.cwd: "${workspaceFolder}"`로 우회 (ADR-020 함정 클래스). **합류자 영향**: 자동 동작 — pull 직후 Ctrl+Shift+B/F5 즉시 사용 가능. 단 *첫 F5* 시 C# Dev Kit extension 설치 필요 (권장 목록에 박혀있음, 본인이 Install). Phase 06 부산물로 박힘. 공통 빌드/실행 셋업 변경이라 [M]. | [M] |
| 2026-05-16 | **정유현 영역 신설 + 슬러그 통일(`yuhyun` → `yuhyeon`)**. 신설: `01_Phases/yuhyeon/README.md` (학습 목표·트랙·팀장 메모·권장 흐름 시드), `00_Document/learning-journal/yuhyeon/` (`.gitkeep` + 짧은 README). 슬러그 정정 5건: `CODEOWNERS` 3행(폴더 + GitHub username placeholder), `learning-journal/README.md`, `.claude/templates/CONTEXT-template.md`, `.claude/setup-steps/01-intro.md`. PR #11(`01_Phases/` 영역 분리)에서 `yuhyeon` 슬러그 박힌 뒤 기존 자리잡이 `yuhyun`과 어긋난 상태 정정. 마일스톤 폴더는 *유현 본인이 `/work:plan`으로* 만들도록 비워둠(각자 phase 가시화 의도). 영역 신설 + placeholder username 정정이라 [L]. | [L] |
| 2026-05-16 | **`00_Document/learning-journal/` 정책 변경 — 비공개 → 공개**. 기존: `learning-journal/*/` ignore (사람별 폴더 비공개, 템플릿·README만 화이트리스트). 새: ignore 패턴 통째 제거, 모두 기본 공개. **이유**: 팀 학습 환경 공유 + 합류자 참고 자료 + 포트폴리오 어필. **영역 자율**: 비공개 원하면 본인이 자기 네임스페이스를 `.gitignore`에 직접 추가 (예: `00_Document/learning-journal/<자기 슬러그>/`). 본인 일지 2건 자동 추가 (`youngho/M1-foundation/01-solution-bootstrap.md`, `youngho/M1-foundation/concepts/dotnet-standard-and-embedded-pdb.md`). **합류자 영향**: 정유현/인규가 자기 첫 일지 만들 때 *자동 공개됨* — 슬랙/디스코드 미리 안내 권장. 위험도 [H] (영역 정책 뒤집기, 사적 영역이 공개로 전환). | [H] |
| 2026-05-16 | **`01_Phases/` 사람별 네임스페이스 분리** — 구조: `01_Phases/<본인 네임스페이스>/M{N}-{slug}/NN-*.md`. `learning-journal/<본인 네임스페이스>/` 패턴과 일관 (사람이 상위, 마일스톤이 그 안). 본인 작업물 27개 .md + 2개 .html 모두 `01_Phases/youngho/M{N}/`로 `git mv` (history 보존). 본인 의도 = 팀원이 자기 phase 만들 때 본인 phase와 겹침 차단. 정유현이 자기 phase 만들면 `01_Phases/yuhyeon/M{N}/...`, 인규는 `01_Phases/ingyu/...` 식. `01_Phases/README.md` + `_template.md`는 공유 그대로. 동반 갱신: CLAUDE.md 헌법 / PRD.md / commands-index.md / pin-and-done.md / ADR-013 / done-md-template.md (2건 + 실제 사례 4줄) / CONTEXT-template.md (2건) / commands/work/plan.md (2건) / commands/journal/phase.md (2건) / team-guide.html FAQ — 모두 `<본인 네임스페이스>` placeholder 또는 `youngho` 실제 경로로 갱신. 옛 빈 마일스톤 폴더(`01_Phases/M1-foundation/`, `01_Phases/M2-first-connection/`) 자동 정리. 훅(`validate-phase-gate.sh`)에 경로 박힘 X — 안전. **영역 분리 변경이라 [H]** (본인 룰: 영역 분리는 [H]). 정유현 pull 시 정유현 머신에서 본인 작업물이 `youngho/` 안에 있는 걸로 보임 (학습 자료 활용 OK). | [H] |
| 2026-05-16 | `/session:end` 동작 변경 — 단계 7.5 신설로 CONTEXT.md 자동 갱신 흐름 박힘. 자동 갱신 범위: "⏸️ 현재 멈춤 지점" 한 문단 + "학습 일지 후보" 1~2줄 + CONTEXT_History.md 이력 한 줄. 자동 갱신 X: 다른 섹션·응축(큰 마일스톤 때만 사용자 결정). 미리보기 → "OK/스킵/수정" 컨펌 후 Edit. `.gitignore`된 본인 자산 안전망(7.5-A). 동반: 본 커맨드 학부생 안내문 응축(264→242줄, -22), `doc-thresholds.md` "220줄 임계" 표에 슬래시 커맨드 행 명시화(응축 가능형 동일 룰), `commands-index.md` 두 곳 동기화. ad-hoc 케이스(session:log 단독 호출)는 본 변경 효과 본 후 실측 기반 별도 추가 결정. 매 세션 마감마다 본인이 "CONTEXT 갱신해줘" 손으로 부탁하던 부담 자동화. 모든 팀원 Phase 마감 시 호출 슬래시 동작 변경이라 [M]. | [M] |
| 2026-05-16 | `.gitattributes` 신설 (Unity YAML 풀세트 LF + .NET 솔루션 CRLF + 바이너리 명시). Windows + `core.autocrlf=true` 환경에서 Unity가 `.asset` 파일을 같은 내용으로 다시 저장 → Git stat이 touch 감지 → phantom dirty 트리거 차단. 트리거 사건: 정유현 환경에서 `EditorBuildSettings.asset` + `ShaderGraphSettings.asset`이 빈 diff + "LF will be replaced by CRLF" 경고만으로 떴음 (ad-hoc Unity 6000.4.7f1 동기화 후속). 본 파일 없으면 매 Unity 실행마다 누군가 (C-3) STOP에 걸려 일을 못 시작. 동반: 학습 보존 (8) — Unity 프로젝트 Windows 함정 = `.gitattributes` 없으면 autocrlf=true가 phantom diff 만드는 패턴. PR #8. | [M] |
| 2026-05-16 | Unity Editor 버전 6000.4.1f1 → 6000.4.7f1 동기화. revision hash 통일(`f3c3c4248748`). 발견: 같은 라벨 6000.4.1f1인데 본인 hash(`8535861f39e1`)와 정유현 hash(`336a400b9ea2`) 어긋남 → Hub UI에 hash 검색 기능 없는 한계 우회 위해 `unityhub://VERSION/HASH` 딥링크로 통일. 변경: setup-steps/03-unity-client.md(설치 + hash 검증 절차 박제), 04-finalize.md(검증 보고 1곳), README.md(2곳), team-guide.html(2곳 + "막혔을 때" 표 2행 추가 + 푸터 v1.2), CONTEXT.md 보류 중에 (B2) 6.4 minor 점프 재논의 메모. Known Issue UUM-139557(Image Inspector NRE, 6000.4.7f1에도 잔존) 회피 가이드 동반. 정유현(이미 합류, 사용 중)이 실제로 재설치해야 하고 setup 가이드 동작 변경이라 [M]. | [M] |
| 2026-05-15 | `/session:start` 0단계 게이트에 (B+) 정책 박힘. `ProjectSettings.asset` 변경 시 세 갈래: (C-1) cloud 라인만 → 자동 정리, (C-2) cloud + 다른 변경 → STOP+분리 옵션, (C-3) cloud 변경 X → 기존 STOP. 각자 Unity Cloud 계정(Unity AI 토큰 분리) + cloud 라인 commit 차단을 게이트가 자동 처리. 동반 갱신: `team-guide.html` "막혔을 때" 표에 한 행 박제. 모든 팀원 매일 첫 호출 슬래시 동작 변경이라 [M]. | [M] |
| 2026-05-15 | `/session:start` STOP 메시지 (B)·(C)에 "처음 보는 STOP이면 CHANGELOG 최상단 / 가이드 3번 섹션 박스 보세요" 한 줄 추가 + 가이드 3번 섹션(`00_Document/team-guide.html`) 기존 ⚠ 안전 매뉴얼 박스 아래에 💡 "갑자기 STOP 떴어요? — 하네스 변경 후 첫 세션" note 콜아웃 신설 (3단계 처리 안내 + [H] 변경 슬랙 동반 안내). 같은 날 [H] 게이트 변경의 후속 — 옛→새 전환 시점에 학부생 백지가 패닉 안 하도록 컨텍스트 경로 명시. 동작 변경 아니라 안내 문구 추가라 [M]. | [M] |
| 2026-05-15 | `/session:start` git 안전 게이트 신설 ([0단계] 추가): `git status --porcelain=v1 --branch`로 브랜치+워킹 상태 점검, main 브랜치 또는 uncommitted 변경 시 CONTEXT 읽기 진입 자체 차단 (STOP 메시지 + 해결 안내). 파괴적 명령(`reset --hard`/`checkout .`/`clean -fd`) Claude 실행 절대 금지 명시, 사용자가 요청해도 안내만. 가이드 v1.1 동반 갱신 (`00_Document/team-guide.html`, README에 링크 추가) (다이어그램 순서 뒤집기: `/session:start` 먼저 → 게이트 통과 시 `git pull`, "막혔을 때" 표에 Git 충돌·복구 행 분리, 안전 매뉴얼 콜아웃 박스 신설). 모든 팀원 매일 첫 호출 슬래시 동작 변경이라 [H]. | [H] |
| 2026-05-15 | README 갱신 (Action 1 후속): L1 헌법 표 셀에 `00_Document/policies/` 외부화 명시 ("절대 원칙·라우팅·스택만 / 운영 정책은 외부화"), 추가 인프라 섹션에 `policies/INDEX.md` + `REVIEW_CHECKLIST.md` 2개 항목 신설, 폴더 구조 `00_Document/` 설명에 policies·REVIEW_CHECKLIST 명시. 팀원·외부 독자가 한눈에 헌법/정책 분리 패턴을 보게. | [L] |
| 2026-05-15 | 헌법 응축 (354→175줄, -51%) + 정책 4개 외부화 (`00_Document/policies/`). 신규: `reporting-format.md` (5단계 보고 + work-envelope) / `pin-and-done.md` (current-pin + -DONE.md 박제 + Phase 완료 권유) / `doc-thresholds.md` (220/350줄 임계) / `review-tiering.md` (ADR-019 Tier 2). 헌법 자체는 절대 원칙·라우팅·스택만 유지. 자기참조 정책 루프 해소. 6파일 7건 참조 갱신 (훅·커맨드·템플릿·ADR.md). 훅 grep 패턴↔외부 정책 양식 정합 6항목 확인. | [M] |
| 2026-05-15 | ADR-019 박제: Reviewer 서브에이전트 도입 (Tier 2 자동 리뷰). 신규 `.claude/agents/reviewer.md` + `00_Document/REVIEW_CHECKLIST.md` (5축 점검 매핑) + 헌법 `## Tier 2 자동 리뷰` 섹션 추가. 도메인 에이전트 코드 변경 후 메인 세션이 트리거 조건(새 핸들러/패킷/공식/≥10줄/`98_Shared/` 포함) 충족 시 reviewer 자동 호출. 코드 스타일 검증은 의도적 Scope 제외 (Roslyn analyzer 도입 후보로 미루). 서브에이전트 6→7. | [M] |
| 2026-05-14 | `/session:end` 슬래시 커맨드 신설 (Phase 마감 절차 8단계) + `/session:log` Codex 분기 추가 (Codex 없는 팀원은 Claude 단독 박제) + 헌법 Phase 완료 권유 섹션 통합 (15 → 16 슬래시) + inject-current-pin.sh 훅 확장 (commit 안 된 -DONE.md 경고 주입) | [M] |
| 2026-05-14 | 협업 셋업 인프라 박제: `/setup` 슬래시 커맨드 + setup-steps 5개 + CHANGELOG 시스템 + CODEOWNERS + `/session:start` CHANGELOG 자동 확인 | [M] |
| 2026-05-14 | `.vscode/settings.json` 화이트리스트로 공유 (통합 터미널 Git Bash + 자동 저장 + LF + 저장 시 포맷) | [M] |
| 2026-05-12 | ADR-005 v2: PostgreSQL → MSSQL LocalDB + Windows 통합 인증 정정 | [H] |
| 2026-05-14 | ADR-020 박제: 훅 실행 환경 의존성 (Git Bash + PATH 셋업 필수) | [L] |
| 2026-05-14 | 협업 베이스라인 박제: CONTEXT 시스템 각자 보유 (.gitignore) + learning-journal 사람별 네임스페이스 + `.vscode/extensions.json` 화이트리스트 | [M] |

---

## 이력 외부화 정책

220줄 넘으면 `CHANGELOG_History.md`로 분리 (헌법 ADR-014 — 문서 세분화 정책).
또는 분기별로 자연 분리: `CHANGELOG_2026Q2.md` 등.
