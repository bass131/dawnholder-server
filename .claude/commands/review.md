---
description: 최근 코드 변경이 헌법/ADR/구조를 잘 따르는지 자동 점검
argument-hint: [점검 범위 - 선택. 예: "방금 한 작업", "Phase 03"]
---

사용자가 코드 리뷰를 요청했습니다. 점검 범위: **$ARGUMENTS**
(범위 없으면 가장 최근 변경 사항 자동 감지)

다음 5가지 축으로 자동 점검하세요. 학부생 멘토 톤으로, 발견한 문제는
"틀렸다"가 아니라 "이렇게 하면 더 좋아요"로.

---

### 점검 1: 헌법 준수 (CLAUDE.md)

루트 `CLAUDE.md`의 5대 절대 원칙 점검:

1. **Server Authority** — 클라에서 게임 상태(HP, XP, 인벤토리, 통화) 변경
   하는 코드 있는가? 데미지 계산이 클라에 있는가?
2. **Protocol Sacred** — 새 패킷이 [MessagePackObject] + 명시적 [Key(N)]?
   PacketId 충돌? 기존 패킷 필드 재정렬?
3. **Trust Boundary** — 클라이언트 입력에 대한 검증 코드 있는가?
   범위 체크, 소유권 체크, rate-limit?
4. **Shared Discipline** — 98_Shared/ 변경 시 양쪽 빌드 검증?
   ProtocolVersion bump 필요 여부?
5. **No Blocking in Tick** — 틱 루프에서 await DB / Task.Delay /
   Thread.Sleep 사용?

도메인별 `CLAUDE.md`(03_Client/02_Server/shared)도 적용 범위에 따라 점검.

### 점검 2: ADR 준수 (00_Document/ADR.md)

채택된 ADR과 충돌하는 코드가 있는가?

- 다른 직렬화 라이브러리 사용? (ADR-002와 충돌)
- 98_Shared/ 외부에 패킷 정의? (ADR-003 모노레포 + shared 분리 원칙)
- 비TCP 통신? (ADR-002)
- 다른 ORM이나 DB 직접 접근? (ADR-005)
- 기타 채택된 ADR 위반

### 점검 3: 구조 준수 (00_Document/ARCHITECTURE.md)

디렉토리 구조 점검:

- 파일이 적절한 폴더에 있는가? (예: TCP 코드가 02_Server/Network/에 있는지)
- 의존성 방향 OK? (client는 shared 읽기만, server는 shared 읽고 쓰기 OK)
- 한 Map 안에서 lock 사용? (Actor 모델 위반)

### 점검 4: 테스트 커버리지

일반:
- 새 핸들러에 happy path 테스트 있는가?
- 새 핸들러에 invalid input 테스트 있는가?
- 새 핸들러에 auth 테스트 있는가?
- 테스트 이름이 의도를 명확히 표현하는가?

**테스트 부재 시 🔴(고쳐야 함)으로 표시할 영역** — 게임 서버 도메인에서 테스트 없으면 늦게 폭발하는 곳들:
- **공식(damage / stat / xp 계산)** — 입력→출력이 명확한데 안 쓰면 회귀 시 무성하게 깨짐
- **직렬화 라운드트립**(PDL → 패킷 → PDL) — 호환성 깨짐이 production에서 발견됨
- **상태 머신**(연결/세션/캐릭터 lifecycle) — 엣지 케이스가 많아 머릿속으로 추론 불가

이 외 영역은 🟡(개선 제안)로 짚되, 위 셋은 🔴로 강하게 짚으세요.

### 점검 5: 도메인 적합 코드 패턴 (학습 포인트)

위반은 아니지만 게임 서버 도메인에서 학습 가치가 큰 패턴들. 발견하면 🟡 또는 🎓 학습 포인트로 짚어주기:

- **Composition over inheritance** — 상속 깊이 2단계 이상이거나, "이 클래스가 X도 되고 Y도 된다" 식이면 컴포넌트 분리 제안. 게임 도메인에선 거의 항상 composition이 맞음.
- **Hot path 알로케이션 최소화** — 틱 루프(`02_Server/GameServer/Loop/`) 또는 패킷 처리 경로에서 매 호출마다 `new`, `ToList()`, LINQ 체인, 박싱? 별 문제 없어 보여도 짚어두기 — 부하 테스트 들어가야 비로소 보임.
- **Rule of three (premature abstraction 금지)** — 비슷한 로직이 **2번** 등장한 시점에 인터페이스/베이스 클래스로 추출했다면 너무 이른 추상화. 3번까지 기다리는 게 게임 도메인에선 거의 항상 맞음 (요구사항이 자주 바뀜).
- **명시적 상태 머신** — `if (state == X && other != Y && ...)` 같은 implicit state는 enum + 명시적 transition 함수로. 특히 연결/세션/스킬 lifecycle.
- **YAGNI / scope creep** — `00_Document/PRD.md`의 "MVP 제외" 항목이 슬그머니 들어왔는지? 미래 확장성을 위한 hook이 추가됐는데 현재 호출자가 0인지?

이 항목들은 **위반(🔴)이 아니라 학습 기회(🟡 또는 🎓)**로 다루세요. SOLID 같은 추상 원칙을 외우는 것보다, 실제 코드에서 "어 이거 rule of three 위반이네"를 만나야 몸에 박혀요.

---

### 결과 보고 형식

```
─────────────────────────────────────────
🔍 코드 리뷰 결과
─────────────────────────────────────────

검토 범위: [구체적 파일/Phase/diff]

🟢 잘 된 점 (3~5개)
- [구체적 칭찬, 어떤 원칙을 잘 따랐는지]
- ...

🟡 개선 제안 (헌법/ADR 충돌은 아니지만 더 좋게 만들 수 있는 것)
- [파일:줄] 무엇을 / 왜 더 좋게 만들 수 있는지 / 어떻게
- ...

🔴 위반 사항 (헌법/ADR과 충돌, 고쳐야 함)
- [파일:줄] 어떤 원칙 위반 / 왜 위반인지 / 수정 방향
  (위반 사항은 자세히 설명. 학부생이 이해할 수 있게.)

📊 테스트 커버리지
- 새 코드 N줄 / 테스트로 커버되는 N줄 (대략)
- 누락된 테스트 케이스: [구체적 시나리오들]

🎓 학습 포인트
이번 리뷰에서 짚어두면 좋은 개념 1~2개. 예:
- "여기서 사용한 Channel<T> 패턴은 producer-consumer라고 부릅니다.
  나중에 메시지 큐나 이벤트 버스 만들 때도 같은 패턴이에요."

➡️ 다음 액션 추천
1. 🔴 [최우선 수정]
2. 🟡 [선택 개선]
3. ✅ [통과한 부분 확장 가능성]
```

---

### 중요 원칙

- **잘한 점도 반드시 짚어주세요.** 학습자가 자기 강점을 알아야 자신감이 붙어요.
  "잘한 점 3개" 못 찾으면 더 노력해서 찾으세요.
- **위반 사항은 절대 무시하지 마세요.** 학부생이 자존심 상할까봐 봐주는 거
  최악입니다. 친절하게 설명하되, 명확하게 짚으세요.
- **"이건 취향 차이"인 것은 🟡로 표시.** 헌법/ADR과 진짜 충돌하는 것만 🔴.
- **숨은 함정도 짚으세요.** 지금은 동작하지만 나중에 문제 될 수 있는 것
  (예: "이 코드는 동시 접속 100명까진 OK인데, 1000명 가면 X 때문에 깨질 거예요").
