---
owner: youngho
milestone: M4.3R
title: Code Convention 전체 리팩토링 — 마일스톤 마감 5단계 보고
status: done
grade: 대규모
completed_at: 2026-05-29
---

# M4.3R — Code Convention 전체 리팩토링 (마일스톤 마감 보고)

> **대규모 마일스톤 5단계 보고** (캡스톤 평가 자산). 순수 리팩토링 — 동작 보존 절대, PDL/Protocol 변경 0.
> convention-audit 워크플로우(5에이전트/73파일 감사 → 9 백로그) → `/work:plan` 7 Phase → plan-auditor 🔴0 GO → 자율 실행.

---

## 🎯 무엇을 만들었나

ADR-028로 수립한 Code Convention을 **코드베이스에 처음으로 전면 적용**했다. 핵심 성과:

- **God class 3개 분리** (§2.2 컨테이너+System / §3.2 dispatch):
  | 대상 | before | after | 추출물 |
  |---|---|---|---|
  | `GameMap` (서버) | 665줄 (전투/AI/respawn/broadcast 4도메인) | **260줄** | CombatSystem / EnemyAISystem / RespawnSystem |
  | `UnityClientSession` (클라) | 665줄 (inline switch 11 + 5책임) | **224줄** | IClientPacketHandler+dispatch / RosterTransitionBuffer / SceneRouter |
  | `GameSession` (서버, trust-boundary) | 700줄 | **566줄** | IntentRateLimiter / MapMigration |
- **size-guard 경고 3건 → 0** (세 파일 모두 <600줄)
- **§3.3 네이밍 정합**: 네트워크 레이어 `m_` 헝가리안 → `_camelCase`(필드 17종) + 파라미터 `_prefix` → `camelCase` + 클라 `[SerializeField]` 9파일 `_camelCase`(FormerlySerializedAs) + isPaused
- **테스트 자산 보강**: PlayerPredictorTests 13개(reconcile) + IntentRateLimiterTests 7개

---

## 🤔 왜 필요한가

- **부채 무게중심 제거**: 600줄+ God class는 "한 도메인만 보려 해도 전체를 읽어야 함" = §0.2(읽는 사람 뇌 부담) 정면 위배. Phase 07(M4.3 enemy AI)이 패킷을 *추가하기 전에* dispatch 테이블로 바꿔야 부채가 안 쌓인다(시너지).
- **테스트 가능성 회수**: rate-limit이 socket과 묶여 단독 테스트 불가했음 → IntentRateLimiter 추출로 socket 없이 검증. PlayerPredictor는 순수 추출됐으나 테스트가 없어 §3.1 의도 절반만 실현 → 테스트가 추출을 완성.
- **캡스톤 서사**: "Convention 자가 측정(워크플로우 감사) → 우선순위 백로그 → 리팩토링" = 평가 자산.

---

## 🛠️ 어떻게 만들었나

7 Phase, 의존/위험 순서로 자율 실행 (Coordinator 역할 = 메인 세션, 도메인 Worker + reviewer 동원):

1. **Phase 01** (베이스라인) — §3.3 v4 명문화(서버 공통+SerializeField+파라미터) + 정책 결정(SerializeField 옵션A) + 회귀 베이스라인 스냅샷(test 315/0/4). 코드 변경 0.
2. **Phase 02 ∥ 03 (R1, 병렬)** — 클라 dispatch(client Worker) + GameMap System(server Worker). reviewer Tier 2-A: 🔴 **CS0070**(event 외부 raise) 봉합.
3. **Phase 04** (trust-boundary) — IntentRateLimiter + MapMigration. handshake state는 §0.3로 컨테이너 잔류. reviewer trust-boundary 정밀: 🔴 **0** — rate-limit off-by-one/portal 근접/handshake 게이트/lifecycle race 4항목 byte-for-byte 보존, 검증을 한 곳에 모아 split-validation 안티패턴 회피.
4. **Phase 05+06** (client Worker) — EnemyViewFactory 추출 + PlayerPredictorTests + 네이밍.
5. **Phase 07** (네트워크 prefix) — m_ 필드 + 파라미터 정규화(서버+ClientNet 자매, ADR-012).

**§0.3 과분할 금지 준수**: handshake state / pending spawn static / PacketFormat 템플릿 / _heap 필드는 *분리하지 않음*(사유 박음).

---

## 🧪 테스트 결과

- **server**: `dotnet build Dawnholder.slnx` 경고 0/오류 0. `dotnet test` **322 통과 / 0 실패 / 4 skip** (베이스라인 315 + IntentRateLimiter 7 + PlayerPredictor는 EditMode). **기존 테스트 무수정 통과 = 순수 리팩토링 동작 보존 증명.**
- **trust-boundary**: 기존 22개(RateLimit 4/Handshake 4/StateMachine 6/EnterPortal 5/Lifecycle 7) 무수정 통과.
- **⚠️ client (Unity)**: 03_Client는 헤드리스 컴파일 불가 → reviewer 정적 분석으로 검증(🔴 CS0070 봉합). **사용자 Play-test 의무**(아래 다음 스텝).

---

## ➡️ 다음 스텝

1. **클라 Play-test (사용자)** — Unity Editor에서: (a) 컴파일 에러 0, (b) SerializeField 9파일 Inspector 값 보존(FormerlySerializedAs), (c) 마을→사냥터 roster drain / 적 spawn, (d) Pause 토글.
2. **PR 생성** — 사용자 명시 GO 의무(irreversible). M4.3 발표 묶음과 한 PR vs 별 PR 판단.
3. **Discord 공지** — Convention 리팩토링 추가(누적 6→7).
4. **이월(M4.4)**: 파라미터/지역변수 *casing* 정규화 전면(§4 `.editorconfig`+Roslyn) — 본 마일스톤은 네트워크 레이어 prefix만. target rewind(정밀 전투) 등 기존 M4.4 백로그 유지.

### 학습 키워드
§2.2 컨테이너+System(GPP Component) / §3.2 dispatch 테이블 / trust-boundary 리팩토링 invariant 보존 / split-validation 안티패턴 / C# event raise 캡슐화(CS0070) / 필드-파라미터 rename 충돌(m_↔_param) / 순수 리팩토링 동작 보존(테스트 무수정) / Unity 헤드리스 컴파일 한계

---

## 커밋 이력

| 커밋 | 내용 |
|---|---|
| `400b6b0` | Phase 01 §3.3 v4 + 7 Phase 계획 + 베이스라인 |
| `0c2b59b` | R1 코드 (Phase 02 dispatch + 03 GameMap System) |
| `cdb39a2` | R1 -DONE.md (복잡 2건) |
| `c24d26e` | Phase 04 GameSession trust-boundary |
| `dc959fa` | Phase 04 -DONE.md |
| `a80bb37` | Phase 05+06 클라 (EnemyViewFactory/PlayerPredictorTests/네이밍) |
| `cd1fe38` | Phase 07 네트워크 네이밍 정합 |
